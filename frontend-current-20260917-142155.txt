ORBITAL SIGNAL LAB - FRONTEND CURRENT STATE

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\App.tsx
============================================================
import {
  useState,
} from 'react';

import Dashboard
  from './components/layout/Dashboard';

import type {
  SimulationField,
  SimulationInputs,
} from './types/simulation';

import './App.css';

const DEFAULT_SIMULATION:
  SimulationInputs = {
    initialPower: 100,
    referenceDistance: 500,
    finalDistance: 2000,
    attenuationCoefficient: 2,
    numericalStep: 10,
  };

function App() {
  /*
   * inputs:
   * valores que estão sendo editados.
   */
  const [
    inputs,
    setInputs,
  ] =
    useState<SimulationInputs>(
      DEFAULT_SIMULATION,
    );

  /*
   * simulationInputs:
   * último conjunto de parâmetros
   * realmente executado.
   */
  const [
    simulationInputs,
    setSimulationInputs,
  ] =
    useState<SimulationInputs>(
      DEFAULT_SIMULATION,
    );

  function handleInputChange(
    field: SimulationField,
    value: number,
  ) {
    setInputs(
      (
        currentInputs,
      ) => ({
        ...currentInputs,
        [field]: value,
      }),
    );
  }

  function handleSimulate() {
    setSimulationInputs({
      ...inputs,
    });
  }

  function handleReset() {
    setInputs(
      DEFAULT_SIMULATION,
    );

    setSimulationInputs(
      DEFAULT_SIMULATION,
    );
  }

  return (
    <Dashboard
      inputs={inputs}
      simulationInputs={
        simulationInputs
      }
      onInputChange={
        handleInputChange
      }
      onSimulate={
        handleSimulate
      }
      onReset={
        handleReset
      }
    />
  );
}

export default App;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\App.css
============================================================
#root {
  min-height:
    100vh;
}

/* =========================================================
   OUTER PRESENTATION CANVAS
========================================================= */

.app-shell {
  min-height:
    100vh;

  display:
    flex;

  align-items:
    center;

  justify-content:
    center;

  padding:
    34px;

  background:
    radial-gradient(
      circle at 50% 30%,
      #586168 0%,
      #475159 50%,
      #3d474e 100%
    );

  --shell:
    #071216;

  --surface:
    rgba(
      13,
      28,
      34,
      0.88
    );

  --surface-solid:
    #0d1c22;

  --surface-soft:
    #10242b;

  --text:
    #e6eef0;

  --muted:
    #71868e;

  --border:
    rgba(
      187,
      220,
      229,
      0.09
    );

  --border-strong:
    rgba(
      187,
      220,
      229,
      0.15
    );
}

.app-shell[
  data-theme="light"
] {
  background:
    radial-gradient(
      circle at 50% 28%,
      #f0f4f6,
      #dce4e8 72%
    );

  --shell:
    #edf3f5;

  --surface:
    rgba(
      255,
      255,
      255,
      0.90
    );

  --surface-solid:
    #ffffff;

  --surface-soft:
    #f5f8f9;

  --text:
    #17303a;

  --muted:
    #708189;

  --border:
    rgba(
      37,
      65,
      77,
      0.10
    );

  --border-strong:
    rgba(
      37,
      65,
      77,
      0.17
    );
}

/* =========================================================
   MISSION SHELL
========================================================= */

.mission-shell {
  width:
    min(
      1680px,
      100%
    );

  min-width:
    0;

  overflow:
    hidden;

  border:
    1px solid
    var(--border);

  border-radius:
    10px;

  background:
    var(--shell);

  box-shadow:
    0 28px 70px
    rgba(
      0,
      0,
      0,
      0.28
    );
}

.app-shell[
  data-theme="light"
]
.mission-shell {
  box-shadow:
    0 25px 60px
    rgba(
      40,
      60,
      70,
      0.16
    );
}

/* =========================================================
   HEADER
========================================================= */

.topbar {
  position:
    relative;

  z-index:
    100;

  height:
    64px;

  display:
    grid;

  grid-template-columns:
    1fr
    auto
    1fr;

  align-items:
    center;

  padding:
    0
    18px;

  border-bottom:
    1px solid
    var(--border);

  background:
    rgba(
      6,
      17,
      21,
      0.96
    );

  backdrop-filter:
    blur(
      18px
    );
}

.app-shell[
  data-theme="light"
]
.topbar {
  background:
    rgba(
      255,
      255,
      255,
      0.96
    );
}

.brand {
  display:
    flex;

  align-items:
    center;

  gap:
    10px;

  min-width:
    0;
}

.brand__icon {
  width:
    34px;

  height:
    34px;

  flex:
    0 0 auto;

  display:
    grid;

  place-items:
    center;

  color:
    var(--cyan);

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.18
    );

  border-radius:
    7px;

  background:
    rgba(
      97,
      216,
      220,
      0.05
    );
}

.brand__name {
  color:
    var(--text);

  font-size:
    11px;

  font-weight:
    800;

  letter-spacing:
    0.11em;

  white-space:
    nowrap;
}

.brand__name span {
  margin-left:
    4px;

  color:
    #91a4aa;

  font-weight:
    500;
}

.brand__subtitle {
  margin-top:
    2px;

  color:
    var(--muted);

  font-size:
    7px;

  white-space:
    nowrap;
}

.topbar__navigation {
  display:
    flex;

  align-items:
    center;

  gap:
    3px;

  padding:
    4px;

  border:
    1px solid
    var(--border);

  border-radius:
    8px;

  background:
    rgba(
      255,
      255,
      255,
      0.025
    );
}

.app-shell[
  data-theme="light"
]
.topbar__navigation {
  background:
    #f2f6f7;
}

.topbar__navigation-item {
  min-width:
    85px;

  height:
    31px;

  padding:
    0 13px;

  cursor:
    pointer;

  border-radius:
    5px;

  color:
    var(--muted);

  background:
    transparent;

  font-size:
    7px;

  font-weight:
    700;

  letter-spacing:
    0.08em;

  text-transform:
    uppercase;

  transition:
    160ms ease;
}

.topbar__navigation-item:hover {
  color:
    var(--text);
}

.topbar__navigation-item--active {
  color:
    var(--text);

  background:
    rgba(
      255,
      255,
      255,
      0.07
    );

  box-shadow:
    inset
    2px
    0
    0
    var(--cyan);
}

.app-shell[
  data-theme="light"
]
.topbar__navigation-item--active {
  color:
    #17313a;

  background:
    #ffffff;

  box-shadow:
    0 2px 8px
    rgba(
      40,
      60,
      70,
      0.07
    );
}

.topbar__actions {
  justify-self:
    end;

  display:
    flex;

  align-items:
    center;

  gap:
    9px;
}

.system-status {
  display:
    flex;

  align-items:
    center;

  gap:
    8px;

  color:
    var(--muted);
}

.system-status__signal {
  width:
    29px;

  height:
    29px;

  display:
    grid;

  place-items:
    center;

  color:
    var(--cyan);

  border:
    1px solid
    var(--border);

  border-radius:
    5px;
}

.system-status__label,
.system-status__value {
  display:
    block;
}

.system-status__label {
  color:
    var(--muted);

  font-size:
    6px;

  letter-spacing:
    0.15em;
}

.system-status__value {
  margin-top:
    1px;

  color:
    var(--cyan);

  font-size:
    7px;

  letter-spacing:
    0.13em;
}

.system-status__satellite {
  color:
    var(--muted);
}

.topbar__icon-button {
  width:
    34px;

  height:
    34px;

  display:
    grid;

  place-items:
    center;

  cursor:
    pointer;

  color:
    var(--muted);

  border:
    1px solid
    var(--border);

  border-radius:
    5px;

  background:
    rgba(
      255,
      255,
      255,
      0.025
    );

  transition:
    160ms ease;
}

.topbar__icon-button:hover {
  color:
    var(--cyan);

  border-color:
    rgba(
      97,
      216,
      220,
      0.28
    );
}

/* =========================================================
   MAIN STAGE
========================================================= */

.mission-stage {
  position:
    relative;

  height:
    min(
      850px,
      calc(
        100vh - 132px
      )
    );

  min-height:
    750px;

  overflow:
    hidden;

  background:
    #050e12;
}

/* =========================================================
   GLOBE
========================================================= */

.globe-panel {
  position:
    absolute;

  inset:
    0;

  min-width:
    0;

  overflow:
    hidden;

  background:
    #050e12;
}

.globe-viewport {
  position:
    absolute;

  inset:
    0;

  overflow:
    hidden;

  background:
    radial-gradient(
      circle at 53% 44%,
      rgba(
        52,
        150,
        174,
        0.12
      ),
      transparent
      40%
    ),
    #050e12;
}

.globe-viewport canvas {
  display:
    block;
}

.globe-viewport::after {
  content:
    "";

  position:
    absolute;

  inset:
    0;

  z-index:
    2;

  pointer-events:
    none;

  background:
    linear-gradient(
      to bottom,
      rgba(
        5,
        14,
        18,
        0.05
      ),
      transparent
      58%,
      rgba(
        5,
        14,
        18,
        0.52
      )
      100%
    );
}

.globe-tooltip {
  padding:
    8px 10px;

  color:
    #dfeaec;

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.22
    );

  background:
    rgba(
      5,
      16,
      21,
      0.94
    );

  font-size:
    10px;
}

.globe-distance {
  position:
    absolute;

  z-index:
    8;

  top:
    20px;

  left:
    320px;

  pointer-events:
    none;
}

.globe-distance span,
.globe-distance strong {
  display:
    block;
}

.globe-distance span {
  color:
    #688087;

  font-size:
    6px;

  letter-spacing:
    0.16em;
}

.globe-distance strong {
  margin-top:
    3px;

  color:
    #e0ebed;

  font-size:
    11px;

  font-weight:
    500;
}

.globe-model-status {
  position:
    absolute;

  z-index:
    8;

  top:
    22px;

  right:
    330px;

  display:
    flex;

  align-items:
    center;

  gap:
    7px;

  color:
    #80949a;

  font-size:
    6px;

  letter-spacing:
    0.12em;

  pointer-events:
    none;
}

.globe-hud {
  position:
    absolute;

  z-index:
    8;

  min-width:
    132px;

  display:
    flex;

  align-items:
    center;

  gap:
    8px;

  padding:
    8px 9px;

  color:
    #d8e5e7;

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.12
    );

  border-radius:
    5px;

  background:
    rgba(
      8,
      23,
      29,
      0.72
    );

  backdrop-filter:
    blur(
      10px
    );

  pointer-events:
    none;
}

.globe-hud--satellite {
  top:
    150px;

  right:
    330px;
}

.globe-hud--station {
  left:
    320px;

  bottom:
    285px;
}

.globe-hud__icon {
  width:
    29px;

  height:
    29px;

  display:
    grid;

  place-items:
    center;

  color:
    var(--cyan);

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.14
    );
}

.globe-hud__icon--station {
  color:
    #e2b459;
}

.globe-hud span,
.globe-hud strong {
  display:
    block;
}

.globe-hud span {
  color:
    #71868d;

  font-size:
    6px;

  letter-spacing:
    0.13em;
}

.globe-hud strong {
  margin-top:
    3px;

  color:
    #dce7e9;

  font-size:
    8px;

  font-weight:
    500;
}

.globe-scale {
  position:
    absolute;

  z-index:
    8;

  right:
    326px;

  bottom:
    284px;

  display:
    flex;

  align-items:
    center;

  gap:
    7px;

  color:
    #687d84;

  font-size:
    6px;

  letter-spacing:
    0.12em;

  pointer-events:
    none;
}

.globe-scale span {
  width:
    38px;

  height:
    1px;

  background:
    rgba(
      97,
      216,
      220,
      0.3
    );
}

/* =========================================================
   FLOATING LEFT / RIGHT PANELS
========================================================= */

.mission-overlay {
  position:
    absolute;

  z-index:
    20;

  top:
    22px;
}

.mission-overlay--left {
  left:
    20px;

  width:
    275px;
}

.mission-overlay--right {
  right:
    20px;

  width:
    285px;
}

.panel,
.metrics-section,
.analysis-panel,
.dock-object {
  min-width:
    0;

  overflow:
    hidden;

  color:
    var(--text);

  border:
    1px solid
    var(--border);

  border-radius:
    7px;

  background:
    var(--surface);

  backdrop-filter:
    blur(
      16px
    );
}

.panel__heading,
.section-heading,
.analysis-panel__header {
  min-height:
    52px;

  display:
    flex;

  align-items:
    center;

  justify-content:
    space-between;

  padding:
    11px 13px;

  border-bottom:
    1px solid
    var(--border);
}

.panel__heading h2,
.section-heading h2,
.analysis-panel__header h2 {
  margin:
    3px 0 0;

  color:
    var(--text);

  font-size:
    10px;

  font-weight:
    600;
}

.panel__eyebrow {
  display:
    block;

  color:
    var(--muted);

  font-size:
    6px;

  font-weight:
    700;

  letter-spacing:
    0.16em;

  text-transform:
    uppercase;
}

/* =========================================================
   CONTROL PANEL
========================================================= */

.control-panel {
  display:
    flex;

  flex-direction:
    column;
}

.control-panel__fields {
  display:
    grid;

  grid-template-columns:
    repeat(
      2,
      minmax(
        0,
        1fr
      )
    );

  gap:
    10px;

  padding:
    13px;
}

.control-panel__fields
.control-field:last-child {
  grid-column:
    1 / -1;
}

.control-field {
  min-width:
    0;
}

.control-field__header {
  margin-bottom:
    5px;

  display:
    flex;

  justify-content:
    space-between;

  color:
    var(--muted);

  font-size:
    7px;
}

.control-field__symbol {
  color:
    var(--cyan);

  font-family:
    Georgia,
    serif;

  font-size:
    10px;
}

.control-field__input-wrapper {
  height:
    34px;

  display:
    flex;

  align-items:
    center;

  border:
    1px solid
    var(--border);

  border-radius:
    3px;

  background:
    rgba(
      0,
      0,
      0,
      0.14
    );
}

.app-shell[
  data-theme="light"
]
.control-field__input-wrapper {
  background:
    #f6f9fa;
}

.control-field__input-wrapper:focus-within {
  border-color:
    rgba(
      97,
      216,
      220,
      0.4
    );
}

.control-field__input {
  width:
    100%;

  height:
    100%;

  min-width:
    0;

  padding:
    0 8px;

  color:
    var(--text);

  border:
    0;

  background:
    transparent;

  font-size:
    9px;
}

.control-field__unit {
  padding-right:
    7px;

  color:
    var(--muted);

  font-size:
    7px;
}

.control-panel__actions {
  display:
    flex;

  gap:
    6px;

  padding:
    0 13px 11px;
}

.button {
  height:
    34px;

  display:
    inline-flex;

  align-items:
    center;

  justify-content:
    center;

  gap:
    6px;

  cursor:
    pointer;

  border-radius:
    3px;

  font-size:
    7px;

  font-weight:
    800;

  letter-spacing:
    0.11em;

  text-transform:
    uppercase;
}

.button--primary {
  flex:
    1;

  color:
    #031114;

  background:
    var(--cyan);
}

.button--primary:hover {
  background:
    #81eaec;
}

.button--ghost {
  width:
    35px;

  color:
    var(--muted);

  border:
    1px solid
    var(--border);

  background:
    transparent;
}

.control-panel__footer {
  height:
    33px;

  display:
    flex;

  align-items:
    center;

  gap:
    7px;

  padding:
    0 13px;

  color:
    var(--muted);

  border-top:
    1px solid
    var(--border);

  font-family:
    Georgia,
    serif;

  font-size:
    8px;
}

/* =========================================================
   STATUS
========================================================= */

.status-dot {
  width:
    5px;

  height:
    5px;

  display:
    inline-block;

  flex:
    0 0 auto;

  border-radius:
    50%;

  background:
    #687a80;
}

.status-dot--cyan {
  background:
    var(--cyan);

  box-shadow:
    0 0 8px
    rgba(
      97,
      216,
      220,
      0.75
    );
}

/* =========================================================
   TELEMETRY
========================================================= */

.telemetry-panel {
  display:
    flex;

  flex-direction:
    column;
}

.satellite-identity {
  display:
    flex;

  align-items:
    center;

  gap:
    9px;

  padding:
    12px;

  border-bottom:
    1px solid
    var(--border);
}

.satellite-identity__icon {
  width:
    44px;

  height:
    44px;

  display:
    grid;

  place-items:
    center;

  color:
    var(--cyan);

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.15
    );

  background:
    rgba(
      97,
      216,
      220,
      0.035
    );
}

.satellite-identity__code {
  display:
    block;

  margin-bottom:
    3px;

  color:
    var(--cyan);

  font-size:
    6px;

  letter-spacing:
    0.14em;
}

.satellite-identity strong {
  color:
    var(--text);

  font-size:
    8px;

  font-weight:
    500;
}

.telemetry-status {
  height:
    31px;

  display:
    flex;

  align-items:
    center;

  gap:
    7px;

  padding:
    0 12px;

  color:
    var(--muted);

  border-bottom:
    1px solid
    var(--border);

  font-size:
    6px;

  letter-spacing:
    0.11em;
}

.signal-quality {
  padding:
    11px 12px;

  border-bottom:
    1px solid
    var(--border);
}

.signal-quality__header,
.signal-quality__scale {
  display:
    flex;

  align-items:
    center;

  justify-content:
    space-between;
}

.signal-quality__header {
  margin-bottom:
    7px;

  color:
    var(--muted);

  font-size:
    6px;
}

.signal-quality__value {
  font-size:
    7px;

  letter-spacing:
    0.08em;
}

.signal-quality__value--strong {
  color:
    #63dbba;
}

.signal-quality__value--moderate {
  color:
    #e0bd5e;
}

.signal-quality__value--weak {
  color:
    #e99d59;
}

.signal-quality__value--critical {
  color:
    #e76569;
}

.signal-quality__track {
  height:
    4px;

  overflow:
    hidden;

  border-radius:
    20px;

  background:
    rgba(
      255,
      255,
      255,
      0.08
    );
}

.app-shell[
  data-theme="light"
]
.signal-quality__track {
  background:
    rgba(
      40,
      65,
      75,
      0.10
    );
}

.signal-quality__fill {
  height:
    100%;

  border-radius:
    inherit;

  transition:
    width
    350ms ease;
}

.signal-quality__fill--strong {
  background:
    #63dbba;
}

.signal-quality__fill--moderate {
  background:
    #e0bd5e;
}

.signal-quality__fill--weak {
  background:
    #e99d59;
}

.signal-quality__fill--critical {
  background:
    #e76569;
}

.signal-quality__scale {
  margin-top:
    5px;

  color:
    var(--muted);

  font-size:
    5px;
}

.telemetry-list {
  padding:
    3px 12px;
}

.telemetry-row {
  min-height:
    44px;

  display:
    flex;

  align-items:
    center;

  gap:
    9px;

  border-bottom:
    1px solid
    var(--border);
}

.telemetry-row:last-child {
  border-bottom:
    0;
}

.telemetry-row__icon {
  width:
    27px;

  height:
    27px;

  display:
    grid;

  place-items:
    center;

  color:
    var(--muted);

  border:
    1px solid
    var(--border);
}

.telemetry-row span,
.telemetry-row strong {
  display:
    block;
}

.telemetry-row span {
  color:
    var(--muted);

  font-size:
    6px;
}

.telemetry-row strong {
  margin-top:
    2px;

  color:
    var(--text);

  font-size:
    8px;

  font-weight:
    600;
}

.telemetry-panel__footer {
  margin:
    6px 12px 11px;

  padding:
    9px 10px;

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.12
    );

  background:
    rgba(
      97,
      216,
      220,
      0.02
    );
}

.telemetry-panel__footer span,
.telemetry-panel__footer strong {
  display:
    block;
}

.telemetry-panel__footer span {
  color:
    var(--muted);

  font-size:
    5px;

  letter-spacing:
    0.13em;
}

.telemetry-panel__footer strong {
  margin-top:
    4px;

  color:
    var(--cyan);

  font-family:
    Georgia,
    serif;

  font-size:
    10px;

  font-weight:
    400;
}

/* =========================================================
   BOTTOM MISSION DOCK
========================================================= */

.mission-bottom-dock {
  position:
    absolute;

  z-index:
    30;

  right:
    16px;

  bottom:
    14px;

  left:
    16px;

  height:
    245px;

  display:
    grid;

  grid-template-columns:
    235px
    minmax(
      390px,
      1fr
    )
    minmax(
      470px,
      1.25fr
    );

  gap:
    8px;

  padding:
    8px;

  border:
    1px solid
    var(--border);

  border-radius:
    8px;

  background:
    rgba(
      13,
      28,
      34,
      0.86
    );

  backdrop-filter:
    blur(
      18px
    );
}

.app-shell[
  data-theme="light"
]
.mission-bottom-dock {
  background:
    rgba(
      248,
      251,
      252,
      0.92
    );
}

.dock-object {
  display:
    flex;

  flex-direction:
    column;
}

.dock-object__top {
  display:
    flex;

  align-items:
    center;

  justify-content:
    space-between;

  padding:
    10px 12px;

  border-bottom:
    1px solid
    var(--border);
}

.dock-object__top strong {
  display:
    block;

  margin-top:
    3px;

  color:
    var(--text);

  font-size:
    10px;
}

.dock-object__top
> svg {
  color:
    var(--cyan);
}

.dock-object__visual {
  flex:
    1;

  display:
    grid;

  place-items:
    center;

  color:
    #8498a0;

  background:
    radial-gradient(
      circle,
      rgba(
        97,
        216,
        220,
        0.08
      ),
      transparent
      62%
    );
}

.dock-object__visual
svg {
  transform:
    rotate(
      -17deg
    );
}

.dock-object__stats {
  display:
    grid;

  grid-template-columns:
    1fr 1fr;

  border-top:
    1px solid
    var(--border);
}

.dock-object__stats
> div {
  padding:
    9px 11px;
}

.dock-object__stats
> div:first-child {
  border-right:
    1px solid
    var(--border);
}

.dock-object__stats span,
.dock-object__stats strong {
  display:
    block;
}

.dock-object__stats span {
  color:
    var(--muted);

  font-size:
    5px;

  letter-spacing:
    0.11em;
}

.dock-object__stats strong {
  margin-top:
    3px;

  color:
    var(--text);

  font-size:
    8px;
}

.dock-signal--strong {
  color:
    #63dbba
    !important;
}

.dock-signal--moderate {
  color:
    #e0bd5e
    !important;
}

.dock-signal--weak {
  color:
    #e99d59
    !important;
}

.dock-signal--critical {
  color:
    #e76569
    !important;
}

/* =========================================================
   METRICS
========================================================= */

.metrics-section {
  display:
    flex;

  flex-direction:
    column;
}

.section-heading__status {
  color:
    var(--cyan);

  font-size:
    6px;

  letter-spacing:
    0.12em;
}

.metrics-grid {
  flex:
    1;

  display:
    grid;

  grid-template-columns:
    repeat(
      2,
      minmax(
        0,
        1fr
      )
    );

  gap:
    1px;

  background:
    var(--border);
}

.metric-card {
  position:
    relative;

  min-width:
    0;

  padding:
    10px 11px;

  background:
    var(--surface-solid);
}

.metric-card::before {
  content:
    "";

  position:
    absolute;

  top:
    0;

  left:
    0;

  width:
    28px;

  height:
    2px;

  background:
    var(--cyan);
}

.metric-card--blue::before {
  background:
    var(--blue);
}

.metric-card--amber::before {
  background:
    var(--amber);
}

.metric-card__top {
  display:
    flex;

  align-items:
    center;

  justify-content:
    space-between;

  color:
    var(--muted);

  font-size:
    5px;

  letter-spacing:
    0.12em;
}

.metric-card__icon {
  color:
    var(--muted);
}

.metric-card__value {
  margin-top:
    13px;

  overflow:
    hidden;

  color:
    var(--text);

  font-size:
    clamp(
      15px,
      1.4vw,
      22px
    );

  font-weight:
    300;

  letter-spacing:
    -0.035em;

  text-overflow:
    ellipsis;
}

.metric-card__value span {
  margin-left:
    4px;

  color:
    var(--muted);

  font-size:
    6px;
}

.metric-card__description {
  margin-top:
    6px;

  color:
    var(--muted);

  font-size:
    6px;
}

/* =========================================================
   3D ANALYSIS
========================================================= */

.analysis-panel {
  display:
    flex;

  flex-direction:
    column;
}

.analysis-panel__mode {
  display:
    flex;

  align-items:
    center;

  gap:
    5px;

  color:
    var(--muted);

  font-size:
    6px;
}

.analysis-plot-wrapper {
  position:
    relative;

  flex:
    1;

  min-height:
    0;

  overflow:
    hidden;

  background:
    var(--surface-solid);
}

.analysis-plot {
  width:
    100%;

  height:
    100%;
}

.analysis-plot
.js-plotly-plot,
.analysis-plot
.plot-container,
.analysis-plot
.svg-container {
  width:
    100%
    !important;

  height:
    100%
    !important;
}

.analysis-interaction {
  position:
    absolute;

  z-index:
    5;

  top:
    8px;

  right:
    8px;

  display:
    flex;

  align-items:
    center;

  gap:
    4px;

  padding:
    4px 5px;

  color:
    var(--muted);

  border:
    1px solid
    var(--border);

  background:
    var(--surface);

  font-size:
    5px;

  pointer-events:
    none;
}

.analysis-panel__legend {
  min-height:
    31px;

  display:
    flex;

  align-items:
    center;

  gap:
    12px;

  padding:
    0 9px;

  overflow:
    hidden;

  color:
    var(--muted);

  border-top:
    1px solid
    var(--border);

  font-size:
    5px;

  white-space:
    nowrap;
}

.analysis-panel__legend
> div {
  display:
    flex;

  align-items:
    center;

  gap:
    5px;
}

.analysis-legend {
  width:
    13px;

  height:
    2px;

  display:
    inline-block;
}

.analysis-legend--analytical {
  background:
    #48dce0;
}

.analysis-legend--rk4 {
  background:
    #4f83d7;
}

/* =========================================================
   LIGHT THEME DETAILS
========================================================= */

.app-shell[
  data-theme="light"
]
.panel,
.app-shell[
  data-theme="light"
]
.metrics-section,
.app-shell[
  data-theme="light"
]
.analysis-panel,
.app-shell[
  data-theme="light"
]
.dock-object {
  box-shadow:
    0 4px 14px
    rgba(
      35,
      55,
      65,
      0.05
    );
}

.app-shell[
  data-theme="light"
]
.button--primary {
  color:
    #ffffff;

  background:
    #118d99;
}

.app-shell[
  data-theme="light"
]
.metric-card__value {
  font-weight:
    500;
}

/* =========================================================
   RESPONSIVE
========================================================= */

@media (
  max-width:
    1280px
) {
  .app-shell {
    padding:
      18px;
  }

  .mission-overlay--left {
    width:
      245px;
  }

  .mission-overlay--right {
    width:
      250px;
  }

  .globe-distance {
    left:
      280px;
  }

  .globe-model-status,
  .globe-hud--satellite {
    right:
      290px;
  }

  .globe-hud--station {
    left:
      280px;
  }

  .globe-scale {
    right:
      280px;
  }

  .mission-bottom-dock {
    grid-template-columns:
      200px
      1fr
      1.15fr;
  }
}

@media (
  max-width:
    1050px
) {
  .app-shell {
    display:
      block;

    padding:
      0;

    background:
      var(--shell);
  }

  .mission-shell {
    border:
      0;

    border-radius:
      0;
  }

  .mission-stage {
    height:
      auto;

    min-height:
      0;

    display:
      grid;

    grid-template-columns:
      1fr;

    gap:
      10px;

    padding:
      10px;

    background:
      var(--shell);
  }

  .globe-panel {
    position:
      relative;

    min-height:
      620px;

    border:
      1px solid
      var(--border);

    border-radius:
      7px;
  }

  .globe-viewport {
    position:
      absolute;
  }

  .mission-overlay {
    position:
      relative;

    inset:
      auto;

    width:
      auto;
  }

  .mission-overlay--left {
    grid-row:
      2;
  }

  .mission-overlay--right {
    grid-row:
      3;
  }

  .mission-bottom-dock {
    position:
      relative;

    inset:
      auto;

    height:
      auto;

    grid-template-columns:
      1fr;

    background:
      transparent;

    border:
      0;

    padding:
      0;
  }

  .dock-object,
  .metrics-section,
  .analysis-panel {
    min-height:
      220px;
  }

  .telemetry-list {
    display:
      grid;

    grid-template-columns:
      repeat(
        2,
        1fr
      );
  }

  .globe-distance {
    left:
      20px;
  }

  .globe-model-status {
    right:
      20px;
  }

  .globe-hud--satellite {
    right:
      20px;
  }

  .globe-hud--station {
    left:
      20px;

    bottom:
      60px;
  }

  .globe-scale {
    right:
      20px;

    bottom:
      22px;
  }
}

@media (
  max-width:
    700px
) {
  .topbar {
    grid-template-columns:
      1fr auto;

    padding:
      0 10px;
  }

  .topbar__navigation {
    display:
      none;
  }

  .system-status {
    display:
      none;
  }

  .mission-stage {
    padding:
      7px;
  }

  .globe-panel {
    min-height:
      480px;
  }

  .control-panel__fields {
    grid-template-columns:
      1fr;
  }

  .control-panel__fields
  .control-field:last-child {
    grid-column:
      auto;
  }

  .metrics-grid {
    grid-template-columns:
      1fr;
  }

  .telemetry-list {
    grid-template-columns:
      1fr;
  }

  .globe-hud--satellite {
    top:
      80px;
  }

  .globe-hud--station {
    bottom:
      70px;
  }
}

/* =========================================================
   MODEL WORKSPACE
========================================================= */

.mission-stage--model {
  height:
    min(
      850px,
      calc(
        100vh - 132px
      )
    );

  min-height:
    750px;

  overflow:
    hidden;

  background:
    #071317;
}

.app-shell[data-theme="light"]
.mission-stage--model {
  background:
    #edf3f5;
}

.topbar__navigation-item:disabled {
  cursor:
    not-allowed;

  opacity:
    0.38;
}



============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\index.css
============================================================
:root {
  font-family:
    Inter,
    ui-sans-serif,
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    sans-serif;

  font-synthesis: none;
  text-rendering: optimizeLegibility;

  --cyan: #61d8dc;
  --blue: #4f83d7;
  --amber: #d8a650;
  --danger: #df6368;

  --text: #e5edef;
  --muted: #73868e;

  --border:
    rgba(
      190,
      222,
      230,
      0.09
    );

  --border-strong:
    rgba(
      190,
      222,
      230,
      0.15
    );
}

* {
  box-sizing:
    border-box;
}

html,
body,
#root {
  width: 100%;

  min-width:
    320px;

  min-height:
    100%;

  margin:
    0;
}

body {
  min-height:
    100vh;

  overflow-x:
    hidden;

  color:
    var(--text);

  background:
    #465057;
}

button,
input {
  font:
    inherit;
}

button {
  border:
    0;
}

button,
input {
  outline:
    none;
}

button:focus-visible,
input:focus-visible {
  outline:
    1px solid
    var(--cyan);

  outline-offset:
    2px;
}

::selection {
  color:
    #041114;

  background:
    var(--cyan);
}

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\visual-tuning.css
============================================================
/*
 * ORBITAL SIGNAL LAB
 * Final Visual Tuning
 *
 * Glassmorphism + Globe Exposure
 * inspirado no dashboard de referência.
 */

/* =========================================================
   GLOBE / SPACE EXPOSURE
========================================================= */

.globe-viewport {
  background:
    radial-gradient(
      circle at 52% 45%,
      rgba(75, 181, 211, 0.16),
      transparent 40%
    ),
    radial-gradient(
      circle at 50% 50%,
      rgba(22, 73, 92, 0.14),
      transparent 66%
    ),
    #071216;
}

.globe-viewport canvas {
  display: block;

  filter:
    brightness(1.16)
    saturate(1.11)
    contrast(1.025);
}

/*
 * Luz azul atmosférica adicional.
 */
.globe-viewport::before {
  content: "";

  position: absolute;
  inset: 0;

  z-index: 1;

  pointer-events: none;

  background:
    radial-gradient(
      circle at 52% 48%,
      rgba(103, 211, 233, 0.10) 0%,
      rgba(62, 157, 184, 0.055) 30%,
      transparent 56%
    );

  mix-blend-mode: screen;
}

/*
 * Vinheta mais leve.
 */
.globe-viewport::after {
  background:
    linear-gradient(
      to bottom,
      rgba(5, 14, 18, 0.01),
      transparent 55%,
      rgba(5, 14, 18, 0.30) 100%
    );
}

/* =========================================================
   GLOBAL GLASS SYSTEM
========================================================= */

/*
 * Painéis principais.
 *
 * A combinação de:
 * - rgba
 * - backdrop-filter
 * - borda clara
 * produz o vidro fumê.
 */

.panel,
.metrics-section,
.analysis-panel,
.dock-object {
  position: relative;

  background:
    linear-gradient(
      145deg,
      rgba(36, 63, 73, 0.24) 0%,
      rgba(11, 28, 35, 0.50) 42%,
      rgba(8, 22, 28, 0.58) 100%
    );

  border:
    1px solid
    rgba(173, 216, 226, 0.12);

  box-shadow:
    inset 0 1px 0
      rgba(255, 255, 255, 0.035),
    0 10px 32px
      rgba(0, 0, 0, 0.14);

  backdrop-filter:
    blur(18px)
    saturate(125%);

  -webkit-backdrop-filter:
    blur(18px)
    saturate(125%);
}

/*
 * Brilho muito discreto no topo do vidro.
 */
.panel::before,
.metrics-section::before,
.analysis-panel::before,
.dock-object::before {
  content: "";

  position: absolute;

  top: 0;
  left: 8%;
  right: 8%;

  height: 1px;

  z-index: 5;

  pointer-events: none;

  background:
    linear-gradient(
      90deg,
      transparent,
      rgba(198, 235, 241, 0.11),
      transparent
    );
}

/* =========================================================
   PANEL HEADERS
========================================================= */

.panel__heading,
.section-heading,
.analysis-panel__header {
  position: relative;

  background:
    linear-gradient(
      180deg,
      rgba(26, 49, 58, 0.24),
      rgba(7, 21, 27, 0.08)
    );

  border-bottom:
    1px solid
    rgba(179, 216, 225, 0.075);
}

/*
 * Separador iluminado bem sutil.
 */
.panel__heading::after,
.section-heading::after,
.analysis-panel__header::after {
  content: "";

  position: absolute;

  left: 0;
  bottom: -1px;

  width: 34px;
  height: 1px;

  background:
    rgba(97, 216, 220, 0.43);
}

/* =========================================================
   LEFT CONTROL PANEL GLASS
========================================================= */

.control-panel {
  background:
    linear-gradient(
      145deg,
      rgba(27, 55, 64, 0.37),
      rgba(8, 24, 30, 0.60)
    );
}

.control-field__input-wrapper {
  background:
    rgba(3, 14, 18, 0.37);

  border-color:
    rgba(161, 202, 213, 0.105);

  box-shadow:
    inset 0 1px 6px
      rgba(0, 0, 0, 0.13);

  backdrop-filter:
    blur(8px);

  -webkit-backdrop-filter:
    blur(8px);
}

.control-field__input-wrapper:hover {
  border-color:
    rgba(97, 216, 220, 0.20);

  background:
    rgba(5, 20, 25, 0.48);
}

.control-field__input-wrapper:focus-within {
  border-color:
    rgba(97, 216, 220, 0.50);

  background:
    rgba(6, 24, 29, 0.56);

  box-shadow:
    0 0 0 2px
      rgba(97, 216, 220, 0.045),
    inset 0 1px 8px
      rgba(0, 0, 0, 0.13);
}

/* =========================================================
   RIGHT TELEMETRY PANEL
========================================================= */

.telemetry-panel {
  background:
    linear-gradient(
      150deg,
      rgba(27, 54, 64, 0.36),
      rgba(8, 24, 30, 0.59)
    );
}

.satellite-identity {
  background:
    linear-gradient(
      90deg,
      rgba(97, 216, 220, 0.018),
      rgba(255, 255, 255, 0.012)
    );
}

.satellite-identity__icon {
  background:
    radial-gradient(
      circle,
      rgba(97, 216, 220, 0.105),
      rgba(97, 216, 220, 0.015) 65%
    );

  border-color:
    rgba(97, 216, 220, 0.20);

  box-shadow:
    inset 0 0 14px
      rgba(97, 216, 220, 0.035);
}

.telemetry-row__icon {
  background:
    rgba(4, 18, 23, 0.22);

  border-color:
    rgba(166, 206, 216, 0.10);
}

.telemetry-panel__footer {
  background:
    linear-gradient(
      90deg,
      rgba(97, 216, 220, 0.025),
      rgba(97, 216, 220, 0.055)
    );

  border-color:
    rgba(97, 216, 220, 0.15);
}

/* =========================================================
   SIGNAL BAR
========================================================= */

.signal-quality__track {
  background:
    rgba(255, 255, 255, 0.055);

  box-shadow:
    inset 0 1px 3px
      rgba(0, 0, 0, 0.30);
}

/* =========================================================
   BOTTOM GLASS DOCK
========================================================= */

.mission-bottom-dock {
  background:
    linear-gradient(
      135deg,
      rgba(26, 49, 58, 0.48),
      rgba(9, 23, 29, 0.65)
    );

  border:
    1px solid
    rgba(182, 218, 227, 0.11);

  box-shadow:
    inset 0 1px 0
      rgba(255, 255, 255, 0.035),
    0 14px 38px
      rgba(0, 0, 0, 0.18);

  backdrop-filter:
    blur(22px)
    saturate(125%);

  -webkit-backdrop-filter:
    blur(22px)
    saturate(125%);
}

/*
 * Painéis dentro da dock precisam ser
 * ainda mais transparentes para não
 * parecerem caixas dentro de outra caixa.
 */

.mission-bottom-dock
.dock-object,
.mission-bottom-dock
.metrics-section,
.mission-bottom-dock
.analysis-panel {
  background:
    linear-gradient(
      145deg,
      rgba(21, 47, 56, 0.30),
      rgba(7, 22, 27, 0.40)
    );

  box-shadow:
    inset 0 1px 0
      rgba(255, 255, 255, 0.025);

  backdrop-filter:
    blur(12px);

  -webkit-backdrop-filter:
    blur(12px);
}

/* =========================================================
   METRIC CARDS
========================================================= */

.metrics-grid {
  background:
    rgba(170, 209, 219, 0.055);
}

.metric-card {
  background:
    linear-gradient(
      145deg,
      rgba(14, 35, 43, 0.40),
      rgba(6, 21, 27, 0.27)
    );

  backdrop-filter:
    blur(10px);

  -webkit-backdrop-filter:
    blur(10px);
}

.metric-card:hover {
  background:
    linear-gradient(
      145deg,
      rgba(22, 46, 55, 0.48),
      rgba(8, 25, 31, 0.33)
    );
}

.metric-card__value {
  text-shadow:
    0 0 15px
    rgba(255, 255, 255, 0.04);
}

/* =========================================================
   ISS MINI PANEL
========================================================= */

.dock-object__visual {
  background:
    radial-gradient(
      circle at center,
      rgba(97, 216, 220, 0.075),
      rgba(97, 216, 220, 0.015) 43%,
      transparent 70%
    );
}

.dock-object__visual svg {
  filter:
    drop-shadow(
      0 0 9px
      rgba(97, 216, 220, 0.11)
    );
}

/* =========================================================
   PLOTLY CONTAINER
========================================================= */

.analysis-plot-wrapper {
  background:
    linear-gradient(
      145deg,
      rgba(8, 26, 32, 0.30),
      rgba(5, 20, 25, 0.48)
    );
}

.analysis-interaction {
  color:
    #91a7ad;

  background:
    rgba(8, 23, 29, 0.55);

  border-color:
    rgba(171, 211, 220, 0.10);

  backdrop-filter:
    blur(9px);

  -webkit-backdrop-filter:
    blur(9px);
}

.analysis-panel__legend {
  background:
    rgba(4, 17, 22, 0.22);

  border-top-color:
    rgba(170, 209, 219, 0.065);
}

/* =========================================================
   FLOATING GLOBE HUD
========================================================= */

.globe-hud {
  border-color:
    rgba(97, 216, 220, 0.18);

  background:
    linear-gradient(
      145deg,
      rgba(24, 50, 59, 0.47),
      rgba(5, 21, 27, 0.64)
    );

  box-shadow:
    inset 0 1px 0
      rgba(255, 255, 255, 0.025),
    0 8px 24px
      rgba(0, 0, 0, 0.17);

  backdrop-filter:
    blur(14px)
    saturate(120%);

  -webkit-backdrop-filter:
    blur(14px)
    saturate(120%);
}

.globe-hud strong {
  color:
    #f0f7f8;
}

.globe-distance strong {
  color:
    #f2f8f9;
}

.globe-model-status {
  color:
    #a6bac0;
}

/* =========================================================
   ICON LIGHT
========================================================= */

.globe-hud--satellite
.globe-hud__icon {
  color:
    #7ce8eb;

  filter:
    drop-shadow(
      0 0 5px
      rgba(97, 216, 220, 0.40)
    );
}

.globe-hud--station
.globe-hud__icon {
  color:
    #efbf58;

  filter:
    drop-shadow(
      0 0 5px
      rgba(239, 191, 88, 0.32)
    );
}

/* =========================================================
   HEADER GLASS
========================================================= */

.topbar {
  background:
    rgba(5, 17, 22, 0.82);

  backdrop-filter:
    blur(22px)
    saturate(125%);

  -webkit-backdrop-filter:
    blur(22px)
    saturate(125%);

  box-shadow:
    inset 0 -1px 0
      rgba(181, 220, 229, 0.05);
}

.topbar__navigation {
  background:
    rgba(29, 50, 59, 0.40);

  backdrop-filter:
    blur(12px);

  -webkit-backdrop-filter:
    blur(12px);
}

.topbar__navigation-item--active {
  background:
    linear-gradient(
      145deg,
      rgba(92, 126, 138, 0.26),
      rgba(41, 67, 76, 0.28)
    );
}

/* =========================================================
   LIGHT THEME GLASS
========================================================= */

.app-shell[data-theme="light"]
.panel,
.app-shell[data-theme="light"]
.metrics-section,
.app-shell[data-theme="light"]
.analysis-panel,
.app-shell[data-theme="light"]
.dock-object {
  background:
    linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.72),
      rgba(234, 242, 245, 0.57)
    );

  border-color:
    rgba(46, 80, 92, 0.12);

  box-shadow:
    inset 0 1px 0
      rgba(255, 255, 255, 0.82),
    0 10px 28px
      rgba(35, 55, 65, 0.07);

  backdrop-filter:
    blur(19px)
    saturate(112%);

  -webkit-backdrop-filter:
    blur(19px)
    saturate(112%);
}

.app-shell[data-theme="light"]
.mission-bottom-dock {
  background:
    linear-gradient(
      145deg,
      rgba(249, 252, 253, 0.75),
      rgba(229, 238, 242, 0.66)
    );

  border-color:
    rgba(42, 74, 86, 0.12);
}

.app-shell[data-theme="light"]
.mission-bottom-dock
.metric-card {
  background:
    rgba(255, 255, 255, 0.48);
}

.app-shell[data-theme="light"]
.control-field__input-wrapper {
  background:
    rgba(255, 255, 255, 0.50);

  border-color:
    rgba(48, 82, 94, 0.13);
}

.app-shell[data-theme="light"]
.topbar {
  background:
    rgba(247, 251, 252, 0.80);

  border-bottom-color:
    rgba(40, 70, 82, 0.10);
}

.app-shell[data-theme="light"]
.topbar__navigation {
  background:
    rgba(231, 239, 242, 0.62);
}

.app-shell[data-theme="light"]
.topbar__navigation-item--active {
  background:
    rgba(255, 255, 255, 0.65);
}

.app-shell[data-theme="light"]
.analysis-plot-wrapper {
  background:
    rgba(247, 250, 251, 0.55);
}

/*
 * O globo continua escuro mesmo no tema light.
 */
.app-shell[data-theme="light"]
.globe-viewport {
  background:
    radial-gradient(
      circle at 52% 45%,
      rgba(75, 181, 211, 0.17),
      transparent 41%
    ),
    #071216;
}

.app-shell[data-theme="light"]
.globe-viewport canvas {
  filter:
    brightness(1.18)
    saturate(1.10)
    contrast(1.025);
}

/* =========================================================
   LARGE PRESENTATION SCREEN
========================================================= */

@media (min-width: 1400px) {
  .panel,
  .metrics-section,
  .analysis-panel,
  .dock-object {
    backdrop-filter:
      blur(20px)
      saturate(130%);

    -webkit-backdrop-filter:
      blur(20px)
      saturate(130%);
  }

  .globe-distance span,
  .globe-model-status,
  .globe-scale {
    font-size:
      7px;
  }

  .globe-distance strong {
    font-size:
      12px;
  }

  .globe-hud span {
    font-size:
      7px;
  }

  .globe-hud strong {
    font-size:
      9px;
  }
}

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\layout\Header.tsx
============================================================
import {
  Activity,
  Moon,
  Orbit,
  Satellite,
  Sun,
} from 'lucide-react';

export type AppSection =
  | 'Overview'
  | 'Model'
  | 'Simulation';

type ThemeMode =
  | 'dark'
  | 'light';

interface HeaderProps {
  activeSection:
    AppSection;

  theme:
    ThemeMode;

  onSectionChange: (
    section:
      AppSection,
  ) => void;

  onToggleTheme:
    () => void;
}

const navigation: {
  value:
    AppSection;

  label:
    string;
}[] = [
  {
    value:
      'Overview',

    label:
      'VISÃO GERAL',
  },

  {
    value:
      'Model',

    label:
      'MODELO',
  },

  {
    value:
      'Simulation',

    label:
      'SIMULAÇÃO',
  },
];

function Header({
  activeSection,
  theme,
  onSectionChange,
  onToggleTheme,
}: HeaderProps) {
  return (
    <header className="topbar">
      <div className="brand">
        <div className="brand__icon">
          <Orbit
            size={19}
            strokeWidth={1.7}
          />
        </div>

        <div>
          <div className="brand__name">
            ORBITAL

            <span>
              SIGNAL LAB
            </span>
          </div>

          <div className="brand__subtitle">
            Controle de Missão com
            Equações Diferenciais
          </div>
        </div>
      </div>

      <nav
        className="topbar__navigation"
        aria-label="Navegação principal"
      >
        {navigation.map(
          ({
            value,
            label,
          }) => {
            const isActive =
              activeSection ===
              value;

            return (
              <button
                type="button"
                key={value}
                className={
                  isActive
                    ? 'topbar__navigation-item topbar__navigation-item--active'
                    : 'topbar__navigation-item'
                }
                onClick={() =>
                  onSectionChange(
                    value,
                  )
                }
              >
                {label}
              </button>
            );
          },
        )}
      </nav>

      <div className="topbar__actions">
        <div className="system-status">
          <div className="system-status__signal">
            <Activity
              size={14}
            />
          </div>

          <div>
            <span className="system-status__label">
              SISTEMA
            </span>

            <span className="system-status__value">
              ONLINE
            </span>
          </div>

          <Satellite
            className="system-status__satellite"
            size={17}
            strokeWidth={1.5}
          />
        </div>

        <button
          type="button"
          className="topbar__icon-button"
          onClick={
            onToggleTheme
          }
          title={
            theme === 'dark'
              ? 'Alternar para tema claro'
              : 'Alternar para tema escuro'
          }
          aria-label="Alternar tema de cores"
        >
          {theme === 'dark'
            ? (
              <Sun
                size={16}
              />
            )
            : (
              <Moon
                size={16}
              />
            )}
        </button>
      </div>
    </header>
  );
}

export default Header;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\layout\Dashboard.tsx
============================================================
import {
  useMemo,
  useState,
} from 'react';

import {
  Activity,
  Radio,
  Satellite,
  Sigma,
  TrendingDown,
} from 'lucide-react';

import AnalysisPreview
  from '../charts/AnalysisPreview';

import GlobeScene
  from '../globe/GlobeScene';

import ModelView
  from '../model/ModelView';

import ControlPanel
  from '../simulation/ControlPanel';

import MetricCard
  from '../simulation/MetricCard';

import SatelliteTelemetry
  from '../simulation/SatelliteTelemetry';

import SimulationView
  from '../simulation/SimulationView';

import Header, {
  type AppSection,
} from './Header';

import {
  attenuationPercent,
  generateAnalyticalSolution,
} from '../../lib/ode';

import {
  solveRK4,
} from '../../lib/rk4';

import type {
  SimulationField,
  SimulationInputs,
  SimulationResult,
} from '../../types/simulation';

type ThemeMode =
  | 'dark'
  | 'light';

type SignalLevel =
  | 'STRONG'
  | 'MODERATE'
  | 'WEAK'
  | 'CRITICAL';

interface DashboardProps {
  inputs:
    SimulationInputs;

  simulationInputs:
    SimulationInputs;

  onInputChange: (
    field:
      SimulationField,

    value:
      number,
  ) => void;

  onSimulate:
    () => void;

  onReset:
    () => void;
}

const ISS_IMAGE_PATH =
  `${import.meta.env.BASE_URL}iss.jpg`;

function calculateSimulation(
  inputs:
    SimulationInputs,
): SimulationResult {
  const analyticalPoints =
    generateAnalyticalSolution(
      inputs,
    );

  const numericalPoints =
    solveRK4(
      inputs,
    );

  const analyticalFinal =
    analyticalPoints[
      analyticalPoints.length -
      1
    ];

  const numericalFinal =
    numericalPoints[
      numericalPoints.length -
      1
    ];

  const analyticalPower =
    analyticalFinal?.power ??
    0;

  const numericalPower =
    numericalFinal?.power ??
    0;

  const derivative =
    analyticalFinal?.derivative ??
    0;

  const absoluteError =
    Math.abs(
      analyticalPower -
      numericalPower,
    );

  const relativeErrorPercent =
    analyticalPower !== 0
      ? (
          absoluteError /
          Math.abs(
            analyticalPower,
          )
        ) *
        100
      : 0;

  return {
    analyticalPoints,
    numericalPoints,

    analyticalPower,
    numericalPower,

    attenuationPercent:
      attenuationPercent(
        inputs.initialPower,
        analyticalPower,
      ),

    derivative,

    absoluteError,
    relativeErrorPercent,
  };
}

function Dashboard({
  inputs,
  simulationInputs,
  onInputChange,
  onSimulate,
  onReset,
}: DashboardProps) {
  const [
    activeSection,
    setActiveSection,
  ] =
    useState<AppSection>(
      'Overview',
    );

  const [
    simulationActive,
    setSimulationActive,
  ] =
    useState(
      false,
    );

  const [
    theme,
    setTheme,
  ] =
    useState<ThemeMode>(
      'dark',
    );

  /*
   * IMPORTANTE:
   * resultados são calculados somente
   * com os parâmetros já executados.
   */
  const simulation =
    useMemo(
      () =>
        calculateSimulation(
          simulationInputs,
        ),
      [
        simulationInputs,
      ],
    );

  const signalStrength =
    useMemo(
      () => {
        if (
          simulationInputs.initialPower <=
          0
        ) {
          return 0;
        }

        return Math.max(
          0,
          Math.min(
            1,
            simulation.analyticalPower /
              simulationInputs.initialPower,
          ),
        );
      },
      [
        simulationInputs.initialPower,
        simulation.analyticalPower,
      ],
    );

  const signalLevel:
    SignalLevel =
    signalStrength >= 0.5
      ? 'STRONG'
      : signalStrength >= 0.2
        ? 'MODERATE'
        : signalStrength >= 0.05
          ? 'WEAK'
          : 'CRITICAL';

  const signalLabels:
    Record<
      SignalLevel,
      string
    > = {
      STRONG:
        'FORTE',

      MODERATE:
        'MODERADO',

      WEAK:
        'FRACO',

      CRITICAL:
        'CRÍTICO',
    };

  const signalLabel =
    signalLabels[
      signalLevel
    ];

  function handleSimulate() {
    onSimulate();

    setSimulationActive(
      true,
    );
  }

  function handleReset() {
    onReset();

    setSimulationActive(
      false,
    );
  }

  function handleToggleTheme() {
    setTheme(
      (
        current,
      ) =>
        current === 'dark'
          ? 'light'
          : 'dark',
    );
  }

  return (
    <div
      className="app-shell"
      data-theme={theme}
    >
      <section className="mission-shell">
        <Header
          activeSection={
            activeSection
          }
          theme={
            theme
          }
          onSectionChange={
            setActiveSection
          }
          onToggleTheme={
            handleToggleTheme
          }
        />

        {activeSection ===
        'Model' ? (
          <main className="mission-stage mission-stage--model">
            <ModelView
              inputs={
                simulationInputs
              }
              receivedPower={
                simulation
                  .analyticalPower
              }
            />
          </main>
        ) : activeSection ===
          'Simulation' ? (
          <main className="mission-stage mission-stage--model">
            <SimulationView
              inputs={
                inputs
              }
              simulationInputs={
                simulationInputs
              }
              result={
                simulation
              }
              theme={
                theme
              }
              simulationActive={
                simulationActive
              }
              onChange={
                onInputChange
              }
              onSimulate={
                handleSimulate
              }
              onReset={
                handleReset
              }
            />
          </main>
        ) : (
          <main className="mission-stage">
            <GlobeScene
              inputs={
                simulationInputs
              }
              simulationActive={
                simulationActive
              }
              signalStrength={
                signalStrength
              }
            />

            <div className="mission-overlay mission-overlay--left">
              <ControlPanel
                inputs={
                  inputs
                }
                onChange={
                  onInputChange
                }
                onSimulate={
                  handleSimulate
                }
                onReset={
                  handleReset
                }
              />
            </div>

            <div className="mission-overlay mission-overlay--right">
              <SatelliteTelemetry
                inputs={
                  simulationInputs
                }
                simulationActive={
                  simulationActive
                }
                receivedPower={
                  simulation
                    .analyticalPower
                }
                attenuationPercent={
                  simulation
                    .attenuationPercent
                }
                derivative={
                  simulation
                    .derivative
                }
                relativeErrorPercent={
                  simulation
                    .relativeErrorPercent
                }
              />
            </div>

            <div className="mission-bottom-dock">
              <section className="dock-object">
                <div className="dock-object__top">
                  <div>
                    <span className="panel__eyebrow">
                      OBJETO ORBITAL
                    </span>

                    <strong>
                      ISS
                    </strong>
                  </div>

                  <Satellite
                    size={25}
                    strokeWidth={1.2}
                  />
                </div>

                <div
                  className="dock-object__visual"
                  style={{
                    position:
                      'relative',

                    overflow:
                      'hidden',
                  }}
                >
                  <img
                    src={
                      ISS_IMAGE_PATH
                    }
                    alt="Estação Espacial Internacional"
                    style={{
                      position:
                        'absolute',

                      inset:
                        0,

                      width:
                        '100%',

                      height:
                        '100%',

                      objectFit:
                        'cover',

                      objectPosition:
                        'center',

                      opacity:
                        0.78,
                    }}
                  />

                  <div
                    aria-hidden="true"
                    style={{
                      position:
                        'absolute',

                      inset:
                        0,

                      background:
                        'linear-gradient(180deg, rgba(5, 18, 23, 0.08) 0%, rgba(5, 18, 23, 0.18) 42%, rgba(5, 18, 23, 0.72) 100%)',

                      pointerEvents:
                        'none',
                    }}
                  />

                  <div
                    style={{
                      position:
                        'absolute',

                      left:
                        '12px',

                      bottom:
                        '10px',

                      zIndex:
                        2,

                      display:
                        'flex',

                      alignItems:
                        'center',

                      gap:
                        '6px',

                      color:
                        '#dbe9eb',

                      fontSize:
                        '6px',

                      letterSpacing:
                        '0.14em',

                      textTransform:
                        'uppercase',
                    }}
                  >
                    <span className="status-dot status-dot--cyan" />

                    ISS NASA
                  </div>
                </div>

                <div className="dock-object__stats">
                  <div>
                    <span>
                      ÓRBITA
                    </span>

                    <strong>
                      ~420 km
                    </strong>
                  </div>

                  <div>
                    <span>
                      SINAL
                    </span>

                    <strong
                      className={`dock-signal dock-signal--${signalLevel.toLowerCase()}`}
                    >
                      {signalLabel}
                    </strong>
                  </div>
                </div>
              </section>

              <section className="metrics-section dock-metrics">
                <div className="section-heading">
                  <div>
                    <span className="panel__eyebrow">
                      SIMULAÇÃO
                    </span>

                    <h2>
                      Resultados da EDO
                    </h2>
                  </div>

                  <span className="section-heading__status">
                    {simulationActive
                      ? 'ATIVO'
                      : 'PRÉVIA'}
                  </span>
                </div>

                <div className="metrics-grid">
                  <MetricCard
                    label="POTÊNCIA"
                    value={
                      simulation
                        .analyticalPower
                        .toFixed(
                          3,
                        )
                    }
                    unit="W"
                    description="Potência recebida P(r)"
                    icon={
                      <Radio
                        size={15}
                      />
                    }
                  />

                  <MetricCard
                    label="PERDA"
                    value={
                      simulation
                        .attenuationPercent
                        .toFixed(
                          1,
                        )
                    }
                    unit="%"
                    description="Atenuação do sinal"
                    icon={
                      <TrendingDown
                        size={15}
                      />
                    }
                    accent="amber"
                  />

                  <MetricCard
                    label="dP/dr"
                    value={
                      simulation
                        .derivative
                        .toExponential(
                          2,
                        )
                    }
                    description="Variação do sinal"
                    icon={
                      <Activity
                        size={15}
                      />
                    }
                    accent="blue"
                  />

                  <MetricCard
                    label="ERRO RK4"
                    value={
                      simulation
                        .relativeErrorPercent
                        .toExponential(
                          1,
                        )
                    }
                    unit="%"
                    description="Erro numérico"
                    icon={
                      <Sigma
                        size={15}
                      />
                    }
                  />
                </div>
              </section>

              <AnalysisPreview
                analyticalPoints={
                  simulation
                    .analyticalPoints
                }
                numericalPoints={
                  simulation
                    .numericalPoints
                }
                initialPower={
                  simulationInputs
                    .initialPower
                }
                theme={
                  theme
                }
              />
            </div>
          </main>
        )}
      </section>
    </div>
  );
}

export default Dashboard;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\globe\GlobeScene.tsx
============================================================
import {
  useCallback,
  useEffect,
  useMemo,
  useRef,
  useState,
} from 'react';

import Globe, {
  type GlobeMethods,
} from 'react-globe.gl';

import * as THREE from 'three';

import {
  GLTFLoader,
} from 'three/examples/jsm/loaders/GLTFLoader.js';

import {
  DRACOLoader,
} from 'three/examples/jsm/loaders/DRACOLoader.js';

import {
  RadioTower,
  Satellite,
} from 'lucide-react';

import type {
  SimulationInputs,
} from '../../types/simulation';

interface GlobeSceneProps {
  inputs: SimulationInputs;
  simulationActive: boolean;
  signalStrength: number;
}

interface GroundPoint {
  lat: number;
  lng: number;
  altitude: number;
  radius: number;
  color: string;
  label: string;
}

interface SignalArc {
  startLat: number;
  startLng: number;
  startAltitude: number;

  endLat: number;
  endLng: number;
  endAltitude: number;

  maxAltitude: number;

  color: string[];
  stroke: number;

  dashLength: number;
  dashGap: number;
  dashAnimateTime: number;
}

interface OrbitPoint {
  lat: number;
  lng: number;
  altitude: number;
}

interface OrbitPath {
  points: OrbitPoint[];
  color: string;
  stroke: number;

  dashLength: number;
  dashGap: number;
  dashAnimateTime: number;

  label: string;
}

interface IssPosition {
  lat: number;
  lng: number;
}

interface IssObjectData {
  lat: number;
  lng: number;
  altitude: number;

  label: string;

  rotation: {
    x?: number;
    y?: number;
    z?: number;
  };

  modelState: string;
}

type ModelStatus =
  | 'loading'
  | 'ready'
  | 'error';

const EARTH_RADIUS_KM =
  6371;

const ISS_ALTITUDE_KM =
  420;

const ISS_ALTITUDE =
  ISS_ALTITUDE_KM /
  EARTH_RADIUS_KM;

const ISS_ORBIT_INCLINATION =
  51.6;

const ISS_MODEL_PATH =
  `${import.meta.env.BASE_URL}models/iss-b.glb`;

const DRACO_DECODER_PATH =
  'https://www.gstatic.com/draco/versioned/decoders/1.5.7/';

const INITIAL_ISS_POSITION:
  IssPosition = {
    lat: 0,
    lng: -42,
  };

const STATION_POSITION = {
  lat: -2.53,
  lng: -44.30,
};

function wrapLongitude(
  longitude: number,
): number {
  return (
    ((longitude + 180) %
      360 +
      360) %
      360 -
    180
  );
}

function createFallbackIss():
  THREE.Object3D {
  const station =
    new THREE.Group();

  const moduleMaterial =
    new THREE.MeshStandardMaterial({
      color: 0xc5d0d4,
      metalness: 0.65,
      roughness: 0.36,
    });

  const darkMaterial =
    new THREE.MeshStandardMaterial({
      color: 0x17252b,
      metalness: 0.35,
      roughness: 0.48,
    });

  const panelMaterial =
    new THREE.MeshStandardMaterial({
      color: 0x17406b,
      metalness: 0.18,
      roughness: 0.56,
    });

  const truss =
    new THREE.Mesh(
      new THREE.BoxGeometry(
        17,
        0.65,
        0.65,
      ),
      darkMaterial,
    );

  station.add(
    truss,
  );

  for (
    let index = -2;
    index <= 2;
    index += 1
  ) {
    const module =
      new THREE.Mesh(
        new THREE.CylinderGeometry(
          1.25,
          1.25,
          3,
          18,
        ),
        moduleMaterial,
      );

    module.rotation.z =
      Math.PI / 2;

    module.position.x =
      index * 2.2;

    module.position.z =
      index % 2 === 0
        ? 0
        : 0.65;

    station.add(
      module,
    );
  }

  const panelGeometry =
    new THREE.BoxGeometry(
      5.8,
      0.14,
      2,
    );

  [
    -10,
    -6.5,
    6.5,
    10,
  ].forEach(
    (xPosition) => {
      const frontPanel =
        new THREE.Mesh(
          panelGeometry,
          panelMaterial,
        );

      frontPanel.position.set(
        xPosition,
        0,
        2.5,
      );

      station.add(
        frontPanel,
      );

      const rearPanel =
        frontPanel.clone();

      rearPanel.position.z =
        -2.5;

      station.add(
        rearPanel,
      );
    },
  );

  const mast =
    new THREE.Mesh(
      new THREE.BoxGeometry(
        0.45,
        4.2,
        0.45,
      ),
      darkMaterial,
    );

  mast.position.y =
    2.2;

  station.add(
    mast,
  );

  station.rotation.y =
    Math.PI / 2;

  return station;
}

function prepareIssModel(
  source: THREE.Object3D,
): THREE.Object3D {
  const model =
    source.clone(
      true,
    );

  model.traverse(
    (child) => {
      if (
        !(
          child instanceof
          THREE.Mesh
        )
      ) {
        return;
      }

      child.castShadow =
        false;

      child.receiveShadow =
        false;

      if (
        Array.isArray(
          child.material,
        )
      ) {
        child.material =
          child.material.map(
            (material) =>
              material.clone(),
          );
      }
      else {
        child.material =
          child.material.clone();
      }
    },
  );

  const box =
    new THREE.Box3()
      .setFromObject(
        model,
      );

  const size =
    box.getSize(
      new THREE.Vector3(),
    );

  const largestDimension =
    Math.max(
      size.x,
      size.y,
      size.z,
    );

  if (
    Number.isFinite(
      largestDimension,
    ) &&
    largestDimension > 0
  ) {
    const desiredSize =
      18;

    const scale =
      desiredSize /
      largestDimension;

    model.scale.setScalar(
      scale,
    );
  }

  const scaledBox =
    new THREE.Box3()
      .setFromObject(
        model,
      );

  const center =
    scaledBox.getCenter(
      new THREE.Vector3(),
    );

  model.position.sub(
    center,
  );

  const container =
    new THREE.Group();

  container.add(
    model,
  );

  model.rotation.x =
    Math.PI / 2;

  model.rotation.z =
    Math.PI / 2;

  return container;
}

function GlobeScene({
  inputs,
  simulationActive,
  signalStrength,
}: GlobeSceneProps) {
  const containerRef =
    useRef<
      HTMLDivElement | null
    >(null);

  const globeRef =
    useRef<
      GlobeMethods | undefined
    >(undefined);

  const [
    dimensions,
    setDimensions,
  ] = useState({
    width: 900,
    height: 760,
  });

  const [
    modelStatus,
    setModelStatus,
  ] =
    useState<ModelStatus>(
      'loading',
    );

  const [
    issModel,
    setIssModel,
  ] =
    useState<
      THREE.Object3D | null
    >(null);

  const [
    issPosition,
    setIssPosition,
  ] =
    useState<IssPosition>(
      INITIAL_ISS_POSITION,
    );

  const strength =
    useMemo(
      () =>
        Math.max(
          0,
          Math.min(
            1,
            signalStrength,
          ),
        ),
      [
        signalStrength,
      ],
    );

  const fallbackIss =
    useMemo(
      () =>
        createFallbackIss(),
      [],
    );

  useEffect(() => {
    let cancelled =
      false;

    const dracoLoader =
      new DRACOLoader();

    dracoLoader.setDecoderPath(
      DRACO_DECODER_PATH,
    );

    dracoLoader.setDecoderConfig({
      type: 'wasm',
    });

    dracoLoader.preload();

    const loader =
      new GLTFLoader();

    loader.setDRACOLoader(
      dracoLoader,
    );

    setModelStatus(
      'loading',
    );

    loader.load(
      ISS_MODEL_PATH,

      (gltf) => {
        if (
          cancelled
        ) {
          return;
        }

        setIssModel(
          prepareIssModel(
            gltf.scene,
          ),
        );

        setModelStatus(
          'ready',
        );
      },

      undefined,

      (error) => {
        if (
          cancelled
        ) {
          return;
        }

        console.error(
          'Falha ao carregar o modelo 3D da ISS:',
          error,
        );

        setIssModel(
          null,
        );

        setModelStatus(
          'error',
        );
      },
    );

    return () => {
      cancelled =
        true;

      dracoLoader.dispose();
    };
  }, []);

  const issPrototype =
    useMemo(
      () =>
        issModel ??
        fallbackIss,
      [
        issModel,
        fallbackIss,
      ],
    );

  const createIssObject =
    useCallback(
      () =>
        issPrototype.clone(
          true,
        ),
      [
        issPrototype,
      ],
    );

  useEffect(() => {
    if (
      !simulationActive
    ) {
      setIssPosition(
        INITIAL_ISS_POSITION,
      );

      return;
    }

    const startTime =
      performance.now();

    const updatePosition =
      () => {
        const elapsedSeconds =
          (
            performance.now() -
            startTime
          ) /
          1000;

        const orbitalAngle =
          (
            elapsedSeconds *
            3
          ) %
          360;

        const radians =
          THREE.MathUtils
            .degToRad(
              orbitalAngle,
            );

        const latitude =
          ISS_ORBIT_INCLINATION *
          Math.sin(
            radians,
          );

        const longitude =
          wrapLongitude(
            -42 +
            orbitalAngle,
          );

        setIssPosition({
          lat:
            latitude,

          lng:
            longitude,
        });
      };

    updatePosition();

    const interval =
      window.setInterval(
        updatePosition,
        80,
      );

    return () => {
      window.clearInterval(
        interval,
      );
    };
  }, [
    simulationActive,
  ]);

  const stationPoints =
    useMemo<
      GroundPoint[]
    >(
      () => [
        {
          lat:
            STATION_POSITION.lat,

          lng:
            STATION_POSITION.lng,

          altitude:
            0.012,

          radius:
            0.32,

          color:
            '#F0C56E',

          label:
            'Estação Terrestre · São Luís',
        },
      ],
      [],
    );

  const issObjects =
    useMemo<
      IssObjectData[]
    >(
      () => [
        {
          lat:
            issPosition.lat,

          lng:
            issPosition.lng,

          altitude:
            ISS_ALTITUDE,

          label:
            'Estação Espacial Internacional',

          rotation: {
            x: 0,
            y: 0,
            z: 90,
          },

          modelState:
            modelStatus,
        },
      ],
      [
        issPosition,
        modelStatus,
      ],
    );

  const orbitPaths =
    useMemo<
      OrbitPath[]
    >(
      () => {
        const points:
          OrbitPoint[] = [];

        for (
          let index = 0;
          index <= 360;
          index += 1
        ) {
          const radians =
            THREE.MathUtils
              .degToRad(
                index,
              );

          points.push({
            lat:
              ISS_ORBIT_INCLINATION *
              Math.sin(
                radians,
              ),

            lng:
              wrapLongitude(
                -180 +
                index,
              ),

            altitude:
              ISS_ALTITUDE,
          });
        }

        return [
          {
            points,

            color:
              simulationActive
                ? '#568994'
                : '#30474E',

            stroke:
              0.12,

            dashLength:
              0.07,

            dashGap:
              0.022,

            dashAnimateTime:
              simulationActive
                ? 9500
                : 0,

            label:
              'Órbita ilustrativa da ISS',
          },
        ];
      },
      [
        simulationActive,
      ],
    );

  const signalColors =
    useMemo(
      () => {
        if (
          strength >=
          0.5
        ) {
          return [
            '#74F2EE',
            '#53B7EF',
          ];
        }

        if (
          strength >=
          0.2
        ) {
          return [
            '#E8CA65',
            '#6CBFC9',
          ];
        }

        if (
          strength >=
          0.05
        ) {
          return [
            '#E99C54',
            '#D46C54',
          ];
        }

        return [
          '#EA6767',
          '#A9484D',
        ];
      },
      [
        strength,
      ],
    );

  const signalArcs =
    useMemo<
      SignalArc[]
    >(
      () => [
        {
          startLat:
            issPosition.lat,

          startLng:
            issPosition.lng,

          startAltitude:
            ISS_ALTITUDE,

          endLat:
            STATION_POSITION.lat,

          endLng:
            STATION_POSITION.lng,

          endAltitude:
            0.012,

          maxAltitude:
            ISS_ALTITUDE +
            0.035,

          color:
            simulationActive
              ? signalColors
              : [
                  '#374B51',
                  '#273A40',
                ],

          stroke:
            simulationActive
              ? 0.20 +
                strength *
                  0.9
              : 0.18,

          dashLength:
            0.22,

          dashGap:
            0.055,

          dashAnimateTime:
            simulationActive
              ? Math.round(
                  2200 -
                  strength *
                    1100,
                )
              : 0,
        },
      ],
      [
        issPosition,
        simulationActive,
        strength,
        signalColors,
      ],
    );

  useEffect(() => {
    if (
      !containerRef.current
    ) {
      return;
    }

    const updateDimensions =
      () => {
        if (
          !containerRef.current
        ) {
          return;
        }

        setDimensions({
          width:
            containerRef
              .current
              .clientWidth,

          height:
            containerRef
              .current
              .clientHeight,
        });
      };

    updateDimensions();

    const observer =
      new ResizeObserver(
        updateDimensions,
      );

    observer.observe(
      containerRef.current,
    );

    return () => {
      observer.disconnect();
    };
  }, []);

  useEffect(() => {
    const controls =
      globeRef.current
        ?.controls();

    if (
      !controls
    ) {
      return;
    }

    controls.autoRotate =
      !simulationActive;

    controls.autoRotateSpeed =
      0.16;

    controls.enableDamping =
      true;

    controls.dampingFactor =
      0.055;

    controls.minDistance =
      165;

    controls.maxDistance =
      470;

    globeRef.current
      ?.pointOfView(
        {
          lat: 8,
          lng: -40,
          altitude: 1.78,
        },
        900,
      );
  }, [
    simulationActive,
  ]);

  const modelLabel =
    modelStatus ===
    'ready'
      ? 'ISS NASA 3D'
      : modelStatus ===
          'loading'
        ? 'CARREGANDO ISS'
        : 'ISS ALTERNATIVA';

  return (
    <section className="globe-panel">
      <div
        ref={
          containerRef
        }
        className="globe-viewport"
      >
        <Globe
          ref={
            globeRef
          }

          width={
            dimensions.width
          }

          height={
            dimensions.height
          }

          backgroundColor="rgba(0,0,0,0)"

          backgroundImageUrl="https://unpkg.com/three-globe/example/img/night-sky.png"

          globeImageUrl="https://unpkg.com/three-globe/example/img/earth-night.jpg"

          bumpImageUrl="https://unpkg.com/three-globe/example/img/earth-topology.png"

          showAtmosphere

          atmosphereColor="#72DBE7"

          atmosphereAltitude={
            0.15
          }

          showGraticules

          pointsData={
            stationPoints
          }

          pointLat="lat"
          pointLng="lng"
          pointAltitude="altitude"
          pointRadius="radius"
          pointColor="color"

          pointLabel={(
            point,
          ) => {
            const item =
              point as
                GroundPoint;

            return `
              <div class="globe-tooltip">
                ${item.label}
              </div>
            `;
          }}

          objectsData={
            issObjects
          }

          objectLat="lat"
          objectLng="lng"
          objectAltitude="altitude"
          objectRotation="rotation"

          objectThreeObject={
            createIssObject
          }

          objectLabel={(
            object,
          ) => {
            const item =
              object as
                IssObjectData;

            return `
              <div class="globe-tooltip">
                <strong>
                  ${item.label}
                </strong>
                <br />
                Órbita: ~${ISS_ALTITUDE_KM} km
                <br />
                ${item.lat.toFixed(2)}°,
                ${item.lng.toFixed(2)}°
              </div>
            `;
          }}

          pathsData={
            orbitPaths
          }

          pathPoints="points"
          pathPointLat="lat"
          pathPointLng="lng"
          pathPointAlt="altitude"
          pathColor="color"
          pathStroke="stroke"
          pathDashLength="dashLength"
          pathDashGap="dashGap"
          pathDashAnimateTime="dashAnimateTime"
          pathResolution={1}
          pathLabel="label"

          arcsData={
            signalArcs
          }

          arcStartLat="startLat"
          arcStartLng="startLng"
          arcStartAltitude="startAltitude"

          arcEndLat="endLat"
          arcEndLng="endLng"
          arcEndAltitude="endAltitude"

          arcAltitude="maxAltitude"

          arcColor="color"
          arcStroke="stroke"

          arcDashLength="dashLength"
          arcDashGap="dashGap"
          arcDashAnimateTime="dashAnimateTime"
        />

        <div className="globe-distance">
          <span>
            DISTÂNCIA DO MODELO
          </span>

          <strong>
            {inputs.finalDistance.toLocaleString(
              'pt-BR',
            )}
            {' '}
            km
          </strong>
        </div>

        <div className="globe-model-status">
          <span className="status-dot status-dot--cyan" />

          {modelLabel}
        </div>

        <div className="globe-hud globe-hud--satellite">
          <div className="globe-hud__icon">
            <Satellite
              size={17}
            />
          </div>

          <div>
            <span>
              ISS
            </span>

            <strong>
              órbita ~420 km
            </strong>
          </div>
        </div>

        <div className="globe-hud globe-hud--station">
          <div className="globe-hud__icon globe-hud__icon--station">
            <RadioTower
              size={16}
            />
          </div>

          <div>
            <span>
              ESTAÇÃO TERRESTRE
            </span>

            <strong>
              São Luís · MA
            </strong>
          </div>
        </div>

        <div className="globe-scale">
          ISS NASA

          <span />

          ARRASTE · GIRE · ZOOM
        </div>
      </div>
    </section>
  );
}

export default GlobeScene;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\charts\AnalysisPreview.tsx
============================================================
import Plot from 'react-plotly.js';

import {
  Box,
  MousePointer2,
} from 'lucide-react';

import type {
  SimulationPoint,
} from '../../types/simulation';

type ThemeMode =
  | 'dark'
  | 'light';

interface AnalysisPreviewProps {
  analyticalPoints:
    SimulationPoint[];

  numericalPoints:
    SimulationPoint[];

  initialPower:
    number;

  theme:
    ThemeMode;
}

function buildHoverText(
  points:
    SimulationPoint[],

  initialPower:
    number,

  method:
    string,
): string[] {
  return points.map(
    (point) => {
      const attenuation =
        initialPower > 0
          ? (
              1 -
              point.power /
                initialPower
            ) *
            100
          : 0;

      return [
        `<b>${method}</b>`,
        `<b>Distância:</b> ${point.distance.toFixed(2)} km`,
        `<b>Potência:</b> ${point.power.toFixed(6)} W`,
        `<b>dP/dr:</b> ${point.derivative.toExponential(5)} W/km`,
        `<b>Atenuação:</b> ${attenuation.toFixed(3)} %`,
      ].join(
        '<br>',
      );
    },
  );
}

function AnalysisPreview({
  analyticalPoints,
  numericalPoints,
  initialPower,
  theme,
}: AnalysisPreviewProps) {
  const isLight =
    theme === 'light';

  const textColor =
    isLight
      ? '#4B626C'
      : '#81969E';

  const gridColor =
    isLight
      ? '#D9E2E5'
      : '#1A323A';

  const zeroColor =
    isLight
      ? '#BACBD0'
      : '#29434B';

  const sceneColor =
    isLight
      ? '#F7FAFB'
      : '#08171C';

  const hoverBackground =
    isLight
      ? '#FFFFFF'
      : '#071419';

  const hoverTextColor =
    isLight
      ? '#18313B'
      : '#E5F1F3';

  const analyticalHover =
    buildHoverText(
      analyticalPoints,
      initialPower,
      'Solução analítica',
    );

  const numericalHover =
    buildHoverText(
      numericalPoints,
      initialPower,
      'Solução numérica RK4',
    );

  return (
    <section className="analysis-panel">
      <div className="analysis-panel__header">
        <div>
          <span className="panel__eyebrow">
            ANÁLISE DA EDO
          </span>

          <h2>
            Espaço de Fase 3D
          </h2>
        </div>

        <div className="analysis-panel__mode">
          <Box size={14} />

          X · Y · Z
        </div>
      </div>

      <div className="analysis-plot-wrapper">
        <Plot
          data={[
            {
              type:
                'scatter3d',

              mode:
                'lines',

              name:
                'Analítica',

              x:
                analyticalPoints.map(
                  (
                    point,
                  ) =>
                    point.distance,
                ),

              y:
                analyticalPoints.map(
                  (
                    point,
                  ) =>
                    point.power,
                ),

              z:
                analyticalPoints.map(
                  (
                    point,
                  ) =>
                    point.derivative,
                ),

              text:
                analyticalHover,

              hoverinfo:
                'text',

              line: {
                color:
                  '#48DCE0',

                width:
                  6,
              },
            },

            {
              type:
                'scatter3d',

              mode:
                'lines+markers',

              name:
                'RK4',

              x:
                numericalPoints.map(
                  (
                    point,
                  ) =>
                    point.distance,
                ),

              y:
                numericalPoints.map(
                  (
                    point,
                  ) =>
                    point.power,
                ),

              z:
                numericalPoints.map(
                  (
                    point,
                  ) =>
                    point.derivative,
                ),

              text:
                numericalHover,

              hoverinfo:
                'text',

              line: {
                color:
                  '#4F83D7',

                width:
                  4,
              },

              marker: {
                color:
                  '#6E9BE5',

                size:
                  2.5,

                opacity:
                  0.75,
              },
            },
          ]}
          layout={{
            autosize:
              true,

            paper_bgcolor:
              'rgba(0,0,0,0)',

            plot_bgcolor:
              'rgba(0,0,0,0)',

            margin: {
              l: 0,
              r: 0,
              t: 0,
              b: 0,
            },

            showlegend:
              false,

            hoverlabel: {
              bgcolor:
                hoverBackground,

              bordercolor:
                gridColor,

              font: {
                color:
                  hoverTextColor,

                size:
                  11,
              },
            },

            scene: {
              bgcolor:
                sceneColor,

              camera: {
                eye: {
                  x: 1.55,
                  y: 1.40,
                  z: 0.82,
                },
              },

              xaxis: {
                title: {
                  text:
                    'Distância r',

                  font: {
                    color:
                      textColor,

                    size:
                      9,
                  },
                },

                tickfont: {
                  color:
                    textColor,

                  size:
                    8,
                },

                color:
                  textColor,

                gridcolor:
                  gridColor,

                zerolinecolor:
                  zeroColor,

                backgroundcolor:
                  sceneColor,

                showbackground:
                  true,
              },

              yaxis: {
                title: {
                  text:
                    'Potência P(r)',

                  font: {
                    color:
                      textColor,

                    size:
                      9,
                  },
                },

                tickfont: {
                  color:
                    textColor,

                  size:
                    8,
                },

                color:
                  textColor,

                gridcolor:
                  gridColor,

                zerolinecolor:
                  zeroColor,

                backgroundcolor:
                  sceneColor,

                showbackground:
                  true,
              },

              zaxis: {
                title: {
                  text:
                    'dP/dr',

                  font: {
                    color:
                      textColor,

                    size:
                      9,
                  },
                },

                tickfont: {
                  color:
                    textColor,

                  size:
                    8,
                },

                color:
                  textColor,

                gridcolor:
                  gridColor,

                zerolinecolor:
                  zeroColor,

                backgroundcolor:
                  sceneColor,

                showbackground:
                  true,
              },
            },
          }}
          config={{
            responsive:
              true,

            displaylogo:
              false,

            scrollZoom:
              true,

            modeBarButtonsToRemove: [
              'toImage',
            ],
          }}
          useResizeHandler
          className="analysis-plot"
        />

        <div className="analysis-interaction">
          <MousePointer2
            size={12}
          />

          GIRAR · ZOOM
        </div>
      </div>

      <div className="analysis-panel__legend">
        <div>
          <span className="analysis-legend analysis-legend--analytical" />

          Analítica
        </div>

        <div>
          <span className="analysis-legend analysis-legend--rk4" />

          RK4
        </div>

        <div>
          X · Distância
        </div>

        <div>
          Y · Potência
        </div>

        <div>
          Z · Derivada
        </div>
      </div>
    </section>
  );
}

export default AnalysisPreview;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\simulation\ControlPanel.tsx
============================================================
import {
  Play,
  RotateCcw,
  SlidersHorizontal,
} from 'lucide-react';

import type {
  SimulationField,
  SimulationInputs,
} from '../../types/simulation';

interface ControlPanelProps {
  inputs: SimulationInputs;

  onChange: (
    field: SimulationField,
    value: number,
  ) => void;

  onSimulate: () => void;
  onReset: () => void;
}

interface NumberFieldProps {
  label: string;
  symbol: string;
  unit?: string;
  value: number;
  step?: number;
  min?: number;

  onChange: (
    value: number,
  ) => void;
}

function NumberField({
  label,
  symbol,
  unit,
  value,
  step = 1,
  min = 0,
  onChange,
}: NumberFieldProps) {
  return (
    <label className="control-field">
      <div className="control-field__header">
        <span>
          {label}
        </span>

        <span className="control-field__symbol">
          {symbol}
        </span>
      </div>

      <div className="control-field__input-wrapper">
        <input
          className="control-field__input"
          type="number"
          min={min}
          step={step}
          value={value}
          onChange={(
            event,
          ) =>
            onChange(
              Number(
                event.target.value,
              ),
            )
          }
        />

        {unit && (
          <span className="control-field__unit">
            {unit}
          </span>
        )}
      </div>
    </label>
  );
}

function ControlPanel({
  inputs,
  onChange,
  onSimulate,
  onReset,
}: ControlPanelProps) {
  return (
    <aside className="panel control-panel">
      <div className="panel__heading">
        <div>
          <span className="panel__eyebrow">
            SIMULAÇÃO
          </span>

          <h2>
            Parâmetros do Modelo
          </h2>
        </div>

        <SlidersHorizontal
          size={17}
          strokeWidth={1.5}
        />
      </div>

      <div className="control-panel__fields">
        <NumberField
          label="Potência"
          symbol="P₀"
          unit="W"
          value={
            inputs.initialPower
          }
          min={0.1}
          step={1}
          onChange={(
            value,
          ) =>
            onChange(
              'initialPower',
              value,
            )
          }
        />

        <NumberField
          label="Referência"
          symbol="r₀"
          unit="km"
          value={
            inputs.referenceDistance
          }
          min={1}
          step={10}
          onChange={(
            value,
          ) =>
            onChange(
              'referenceDistance',
              value,
            )
          }
        />

        <NumberField
          label="Distância"
          symbol="r"
          unit="km"
          value={
            inputs.finalDistance
          }
          min={1}
          step={100}
          onChange={(
            value,
          ) =>
            onChange(
              'finalDistance',
              value,
            )
          }
        />

        <NumberField
          label="Coeficiente"
          symbol="k"
          value={
            inputs.attenuationCoefficient
          }
          min={0.1}
          step={0.1}
          onChange={(
            value,
          ) =>
            onChange(
              'attenuationCoefficient',
              value,
            )
          }
        />

        <NumberField
          label="Passo RK4"
          symbol="Δr"
          unit="km"
          value={
            inputs.numericalStep
          }
          min={1}
          step={1}
          onChange={(
            value,
          ) =>
            onChange(
              'numericalStep',
              value,
            )
          }
        />
      </div>

      <div className="control-panel__actions">
        <button
          className="button button--primary"
          type="button"
          onClick={onSimulate}
        >
          <Play
            size={14}
            fill="currentColor"
          />

          SIMULAR
        </button>

        <button
          className="button button--ghost"
          type="button"
          onClick={onReset}
          title="Redefinir parâmetros"
          aria-label="Redefinir parâmetros"
        >
          <RotateCcw
            size={15}
          />
        </button>
      </div>

      <div className="control-panel__footer">
        <span className="status-dot status-dot--cyan" />

        dP/dr = −kP/r
      </div>
    </aside>
  );
}

export default ControlPanel;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\simulation\MetricCard.tsx
============================================================
import type { ReactNode } from 'react';

interface MetricCardProps {
  label: string;
  value: string;
  unit?: string;
  description: string;
  icon: ReactNode;
  accent?: 'cyan' | 'blue' | 'amber';
}

function MetricCard({
  label,
  value,
  unit,
  description,
  icon,
  accent = 'cyan',
}: MetricCardProps) {
  return (
    <article
      className={`metric-card metric-card--${accent}`}
    >
      <div className="metric-card__top">
        <span>{label}</span>

        <div className="metric-card__icon">
          {icon}
        </div>
      </div>

      <div className="metric-card__value">
        {value}

        {unit && (
          <span>{unit}</span>
        )}
      </div>

      <div className="metric-card__description">
        {description}
      </div>
    </article>
  );
}

export default MetricCard;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\simulation\SatelliteTelemetry.tsx
============================================================
import {
  Activity,
  Gauge,
  Navigation2,
  Radio,
  Satellite,
} from 'lucide-react';

import type {
  SimulationInputs,
} from '../../types/simulation';

type SignalQuality =
  | 'STRONG'
  | 'MODERATE'
  | 'WEAK'
  | 'CRITICAL';

interface SatelliteTelemetryProps {
  inputs: SimulationInputs;

  simulationActive: boolean;

  receivedPower: number;
  attenuationPercent: number;
  derivative: number;
  relativeErrorPercent: number;
}

function SatelliteTelemetry({
  inputs,
  simulationActive,
  receivedPower,
  attenuationPercent,
  derivative,
  relativeErrorPercent,
}: SatelliteTelemetryProps) {
  const ratio =
    inputs.initialPower > 0
      ? receivedPower /
        inputs.initialPower
      : 0;

  const normalizedStrength =
    Math.max(
      0,
      Math.min(
        1,
        ratio,
      ),
    );

  const quality:
    SignalQuality =
    normalizedStrength >= 0.5
      ? 'STRONG'
      : normalizedStrength >= 0.2
        ? 'MODERATE'
        : normalizedStrength >= 0.05
          ? 'WEAK'
          : 'CRITICAL';

  const qualityClass =
    quality.toLowerCase();

  const qualityLabels:
    Record<
      SignalQuality,
      string
    > = {
      STRONG:
        'FORTE',

      MODERATE:
        'MODERADO',

      WEAK:
        'FRACO',

      CRITICAL:
        'CRÍTICO',
    };

  return (
    <aside className="panel telemetry-panel">
      <div className="panel__heading">
        <div>
          <span className="panel__eyebrow">
            OBJETO ORBITAL
          </span>

          <h2>
            Status da ISS
          </h2>
        </div>

        <Satellite
          size={18}
          strokeWidth={1.4}
        />
      </div>

      <div className="satellite-identity">
        <div className="satellite-identity__icon">
          <Satellite
            size={31}
            strokeWidth={1.15}
          />
        </div>

        <div>
          <span className="satellite-identity__code">
            ISS · NASA
          </span>

          <strong>
            Estação Espacial Internacional
          </strong>
        </div>
      </div>

      <div className="telemetry-status">
        <span
          className={
            simulationActive
              ? 'status-dot status-dot--cyan'
              : 'status-dot'
          }
        />

        {simulationActive
          ? 'ENLACE ATIVO'
          : 'EM ESPERA'}
      </div>

      <div className="signal-quality">
        <div className="signal-quality__header">
          <span>
            SINAL
          </span>

          <strong
            className={`signal-quality__value signal-quality__value--${qualityClass}`}
          >
            {
              qualityLabels[
                quality
              ]
            }
          </strong>
        </div>

        <div className="signal-quality__track">
          <div
            className={`signal-quality__fill signal-quality__fill--${qualityClass}`}
            style={{
              width:
                `${normalizedStrength * 100}%`,
            }}
          />
        </div>

        <div className="signal-quality__scale">
          <span>
            0%
          </span>

          <span>
            {(
              normalizedStrength *
              100
            ).toFixed(1)}
            %
          </span>
        </div>
      </div>

      <div className="telemetry-list">
        <div className="telemetry-row">
          <div className="telemetry-row__icon">
            <Navigation2
              size={15}
            />
          </div>

          <div>
            <span>
              Distância do modelo
            </span>

            <strong>
              {inputs.finalDistance.toLocaleString()}
              {' '}
              km
            </strong>
          </div>
        </div>

        <div className="telemetry-row">
          <div className="telemetry-row__icon">
            <Radio
              size={15}
            />
          </div>

          <div>
            <span>
              Potência recebida
            </span>

            <strong>
              {receivedPower.toFixed(
                4,
              )}
              {' '}
              W
            </strong>
          </div>
        </div>

        <div className="telemetry-row">
          <div className="telemetry-row__icon">
            <Gauge
              size={15}
            />
          </div>

          <div>
            <span>
              Atenuação
            </span>

            <strong>
              {attenuationPercent.toFixed(
                2,
              )}
              %
            </strong>
          </div>
        </div>

        <div className="telemetry-row">
          <div className="telemetry-row__icon">
            <Activity
              size={15}
            />
          </div>

          <div>
            <span>
              Erro RK4
            </span>

            <strong>
              {relativeErrorPercent.toExponential(
                2,
              )}
              %
            </strong>
          </div>
        </div>
      </div>

      <div className="telemetry-panel__footer">
        <span>
          DERIVADA
        </span>

        <strong>
          {derivative.toExponential(
            3,
          )}
          {' '}
          W/km
        </strong>
      </div>
    </aside>
  );
}

export default SatelliteTelemetry;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\simulation\SimulationView.tsx
============================================================
import {
  Activity,
  Calculator,
  CheckCircle2,
  Gauge,
  Radio,
  Sigma,
} from 'lucide-react';

import AnalysisPreview
  from '../charts/AnalysisPreview';

import type {
  SimulationField,
  SimulationInputs,
  SimulationResult,
} from '../../types/simulation';

import ControlPanel
  from './ControlPanel';

import './SimulationView.css';

type ThemeMode =
  | 'dark'
  | 'light';

interface SimulationViewProps {
  inputs:
    SimulationInputs;

  simulationInputs:
    SimulationInputs;

  result:
    SimulationResult;

  theme:
    ThemeMode;

  simulationActive:
    boolean;

  onChange: (
    field:
      SimulationField,

    value:
      number,
  ) => void;

  onSimulate:
    () => void;

  onReset:
    () => void;
}

function SimulationView({
  inputs,
  simulationInputs,
  result,
  theme,
  simulationActive,
  onChange,
  onSimulate,
  onReset,
}: SimulationViewProps) {
  const inputsChanged =
    inputs.initialPower !==
      simulationInputs.initialPower ||
    inputs.referenceDistance !==
      simulationInputs.referenceDistance ||
    inputs.finalDistance !==
      simulationInputs.finalDistance ||
    inputs.attenuationCoefficient !==
      simulationInputs.attenuationCoefficient ||
    inputs.numericalStep !==
      simulationInputs.numericalStep;

  return (
    <section className="simulation-view">
      <div className="simulation-view__hero">
        <div>
          <span className="simulation-view__eyebrow">
            LABORATÓRIO NUMÉRICO
          </span>

          <h1>
            Simulação da Atenuação
            do Sinal
          </h1>

          <p>
            Compare a solução analítica da
            EDO com a aproximação numérica
            pelo método de Runge-Kutta de
            quarta ordem.
          </p>
        </div>

        <div
          className={
            inputsChanged
              ? 'simulation-view__status simulation-view__status--pending'
              : 'simulation-view__status simulation-view__status--ready'
          }
        >
          {inputsChanged
            ? (
              <>
                <Activity
                  size={16}
                />

                PARÂMETROS ALTERADOS
              </>
            )
            : (
              <>
                <CheckCircle2
                  size={16}
                />

                SIMULAÇÃO ATUALIZADA
              </>
            )}
        </div>
      </div>

      <div className="simulation-view__workspace">
        <div className="simulation-view__controls">
          <ControlPanel
            inputs={inputs}
            onChange={
              onChange
            }
            onSimulate={
              onSimulate
            }
            onReset={
              onReset
            }
          />

          <article className="simulation-snapshot">
            <div className="simulation-snapshot__heading">
              <span>
                ÚLTIMA EXECUÇÃO
              </span>

              <strong>
                Parâmetros utilizados
              </strong>
            </div>

            <div className="simulation-snapshot__grid">
              <div>
                <span>
                  P₀
                </span>

                <strong>
                  {
                    simulationInputs
                      .initialPower
                  }
                  {' '}
                  W
                </strong>
              </div>

              <div>
                <span>
                  r₀
                </span>

                <strong>
                  {
                    simulationInputs
                      .referenceDistance
                  }
                  {' '}
                  km
                </strong>
              </div>

              <div>
                <span>
                  r
                </span>

                <strong>
                  {
                    simulationInputs
                      .finalDistance
                  }
                  {' '}
                  km
                </strong>
              </div>

              <div>
                <span>
                  k
                </span>

                <strong>
                  {
                    simulationInputs
                      .attenuationCoefficient
                  }
                </strong>
              </div>

              <div>
                <span>
                  Δr
                </span>

                <strong>
                  {
                    simulationInputs
                      .numericalStep
                  }
                  {' '}
                  km
                </strong>
              </div>
            </div>

            <div className="simulation-snapshot__equation">
              dP/dr = −kP/r
            </div>
          </article>
        </div>

        <div className="simulation-view__results">
          <div className="simulation-results-grid">
            <article className="simulation-result-card">
              <div className="simulation-result-card__icon">
                <Calculator
                  size={18}
                />
              </div>

              <span>
                SOLUÇÃO ANALÍTICA
              </span>

              <strong>
                {
                  result
                    .analyticalPower
                    .toFixed(
                      6,
                    )
                }
                {' '}
                W
              </strong>

              <small>
                P(r) = P₀(r₀/r)ᵏ
              </small>
            </article>

            <article className="simulation-result-card simulation-result-card--blue">
              <div className="simulation-result-card__icon">
                <Sigma
                  size={18}
                />
              </div>

              <span>
                SOLUÇÃO RK4
              </span>

              <strong>
                {
                  result
                    .numericalPower
                    .toFixed(
                      6,
                    )
                }
                {' '}
                W
              </strong>

              <small>
                aproximação numérica
              </small>
            </article>

            <article className="simulation-result-card simulation-result-card--amber">
              <div className="simulation-result-card__icon">
                <Gauge
                  size={18}
                />
              </div>

              <span>
                ATENUAÇÃO
              </span>

              <strong>
                {
                  result
                    .attenuationPercent
                    .toFixed(
                      3,
                    )
                }
                %
              </strong>

              <small>
                perda relativa de potência
              </small>
            </article>

            <article className="simulation-result-card">
              <div className="simulation-result-card__icon">
                <Radio
                  size={18}
                />
              </div>

              <span>
                ERRO ABSOLUTO
              </span>

              <strong>
                {
                  result
                    .absoluteError
                    .toExponential(
                      3,
                    )
                }
                {' '}
                W
              </strong>

              <small>
                |Panalítico − PRK4|
              </small>
            </article>

            <article className="simulation-result-card simulation-result-card--blue">
              <div className="simulation-result-card__icon">
                <Activity
                  size={18}
                />
              </div>

              <span>
                ERRO RELATIVO
              </span>

              <strong>
                {
                  result
                    .relativeErrorPercent
                    .toExponential(
                      3,
                    )
                }
                %
              </strong>

              <small>
                precisão do método RK4
              </small>
            </article>

            <article className="simulation-result-card">
              <div className="simulation-result-card__icon">
                <Activity
                  size={18}
                />
              </div>

              <span>
                DERIVADA FINAL
              </span>

              <strong>
                {
                  result
                    .derivative
                    .toExponential(
                      4,
                    )
                }
                {' '}
                W/km
              </strong>

              <small>
                taxa instantânea dP/dr
              </small>
            </article>
          </div>

          <div className="simulation-view__chart">
            <AnalysisPreview
              analyticalPoints={
                result
                  .analyticalPoints
              }
              numericalPoints={
                result
                  .numericalPoints
              }
              initialPower={
                simulationInputs
                  .initialPower
              }
              theme={
                theme
              }
            />
          </div>

          <div className="simulation-comparison">
            <div>
              <span>
                MÉTODO ANALÍTICO
              </span>

              <strong>
                Solução exata
              </strong>
            </div>

            <div className="simulation-comparison__connector">
              <span />

              <strong>
                ×
              </strong>

              <span />
            </div>

            <div>
              <span>
                MÉTODO NUMÉRICO
              </span>

              <strong>
                Runge-Kutta 4ª ordem
              </strong>
            </div>

            <div className="simulation-comparison__conclusion">
              {simulationActive
                ? 'Comparação executada'
                : 'Valores iniciais carregados'}
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}

export default SimulationView;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\simulation\SimulationView.css
============================================================
.simulation-view {
  --simulation-bg:
    #071317;

  --simulation-title:
    #edf5f6;

  --simulation-heading:
    #dce8ea;

  --simulation-text:
    #7f949b;

  --simulation-muted:
    #688087;

  --simulation-strong:
    #d3e0e2;

  --simulation-border:
    rgba(
      187,
      220,
      229,
      0.10
    );

  --simulation-card:
    linear-gradient(
      145deg,
      rgba(
        24,
        48,
        57,
        0.43
      ),
      rgba(
        7,
        22,
        28,
        0.58
      )
    );

  --simulation-panel:
    linear-gradient(
      145deg,
      rgba(
        28,
        54,
        63,
        0.38
      ),
      rgba(
        7,
        23,
        29,
        0.63
      )
    );

  --simulation-cell:
    rgba(
      5,
      20,
      25,
      0.68
    );

  height:
    100%;

  overflow-y:
    auto;

  padding:
    24px;

  color:
    var(--text);

  background:
    radial-gradient(
      circle at 75% 5%,
      rgba(
        97,
        216,
        220,
        0.07
      ),
      transparent 35%
    ),
    var(--simulation-bg);
}

.app-shell[
  data-theme="light"
]
.simulation-view {
  --simulation-bg:
    #edf3f5;

  --simulation-title:
    #17303a;

  --simulation-heading:
    #213e48;

  --simulation-text:
    #60757d;

  --simulation-muted:
    #748991;

  --simulation-strong:
    #29434c;

  --simulation-border:
    rgba(
      37,
      65,
      77,
      0.11
    );

  --simulation-card:
    linear-gradient(
      145deg,
      rgba(
        255,
        255,
        255,
        0.94
      ),
      rgba(
        241,
        247,
        249,
        0.88
      )
    );

  --simulation-panel:
    linear-gradient(
      145deg,
      rgba(
        255,
        255,
        255,
        0.92
      ),
      rgba(
        238,
        245,
        247,
        0.88
      )
    );

  --simulation-cell:
    rgba(
      244,
      248,
      250,
      0.96
    );

  background:
    radial-gradient(
      circle at 75% 5%,
      rgba(
        17,
        141,
        153,
        0.08
      ),
      transparent 35%
    ),
    var(--simulation-bg);
}

.simulation-view__hero {
  max-width:
    1360px;

  margin:
    0 auto 16px;

  display:
    flex;

  align-items:
    flex-end;

  justify-content:
    space-between;

  gap:
    24px;
}

.simulation-view__eyebrow {
  color:
    var(--cyan);

  font-size:
    7px;

  font-weight:
    800;

  letter-spacing:
    0.2em;
}

.app-shell[
  data-theme="light"
]
.simulation-view__eyebrow {
  color:
    #118d99;
}

.simulation-view__hero h1 {
  margin:
    7px 0 5px;

  color:
    var(--simulation-title);

  font-size:
    clamp(
      25px,
      3vw,
      40px
    );

  font-weight:
    400;

  letter-spacing:
    -0.035em;
}

.simulation-view__hero p {
  max-width:
    650px;

  margin:
    0;

  color:
    var(--simulation-text);

  font-size:
    10px;

  line-height:
    1.6;
}

.simulation-view__status {
  display:
    flex;

  align-items:
    center;

  gap:
    7px;

  padding:
    9px 11px;

  border:
    1px solid;

  border-radius:
    5px;

  font-size:
    6px;

  font-weight:
    700;

  letter-spacing:
    0.12em;

  white-space:
    nowrap;
}

.simulation-view__status--ready {
  color:
    #48b997;

  border-color:
    rgba(
      72,
      185,
      151,
      0.24
    );

  background:
    rgba(
      72,
      185,
      151,
      0.06
    );
}

.simulation-view__status--pending {
  color:
    #d39b32;

  border-color:
    rgba(
      211,
      155,
      50,
      0.25
    );

  background:
    rgba(
      211,
      155,
      50,
      0.06
    );
}

.simulation-view__workspace {
  max-width:
    1360px;

  margin:
    0 auto;

  display:
    grid;

  grid-template-columns:
    285px
    minmax(
      0,
      1fr
    );

  gap:
    12px;
}

.simulation-view__controls {
  display:
    flex;

  flex-direction:
    column;

  gap:
    12px;
}

.simulation-snapshot {
  overflow:
    hidden;

  border:
    1px solid
    var(--simulation-border);

  border-radius:
    7px;

  background:
    var(--simulation-panel);

  box-shadow:
    inset
    0 1px 0
    rgba(
      255,
      255,
      255,
      0.025
    );

  backdrop-filter:
    blur(
      18px
    );
}

.app-shell[
  data-theme="light"
]
.simulation-snapshot {
  box-shadow:
    0 8px 24px
    rgba(
      38,
      62,
      72,
      0.06
    );
}

.simulation-snapshot__heading {
  padding:
    12px 13px;

  border-bottom:
    1px solid
    var(--simulation-border);
}

.simulation-snapshot__heading span,
.simulation-snapshot__heading strong {
  display:
    block;
}

.simulation-snapshot__heading span {
  color:
    var(--simulation-muted);

  font-size:
    5px;

  letter-spacing:
    0.16em;
}

.simulation-snapshot__heading strong {
  margin-top:
    3px;

  color:
    var(--simulation-heading);

  font-size:
    9px;
}

.simulation-snapshot__grid {
  display:
    grid;

  grid-template-columns:
    repeat(
      2,
      1fr
    );

  gap:
    1px;

  margin:
    12px;

  background:
    var(--simulation-border);
}

.simulation-snapshot__grid
> div {
  padding:
    9px;

  background:
    var(--simulation-cell);
}

.simulation-snapshot__grid span,
.simulation-snapshot__grid strong {
  display:
    block;
}

.simulation-snapshot__grid span {
  color:
    var(--cyan);

  font-family:
    Georgia,
    serif;

  font-size:
    9px;
}

.app-shell[
  data-theme="light"
]
.simulation-snapshot__grid span {
  color:
    #118d99;
}

.simulation-snapshot__grid strong {
  margin-top:
    4px;

  color:
    var(--simulation-strong);

  font-size:
    8px;
}

.simulation-snapshot__equation {
  margin:
    0 12px 12px;

  padding:
    10px;

  text-align:
    center;

  color:
    var(--cyan);

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.12
    );

  background:
    rgba(
      97,
      216,
      220,
      0.025
    );

  font-family:
    Georgia,
    serif;

  font-size:
    13px;
}

.app-shell[
  data-theme="light"
]
.simulation-snapshot__equation {
  color:
    #118d99;

  border-color:
    rgba(
      17,
      141,
      153,
      0.18
    );

  background:
    rgba(
      17,
      141,
      153,
      0.05
    );
}

.simulation-view__results {
  min-width:
    0;

  display:
    flex;

  flex-direction:
    column;

  gap:
    12px;
}

.simulation-results-grid {
  display:
    grid;

  grid-template-columns:
    repeat(
      3,
      minmax(
        0,
        1fr
      )
    );

  gap:
    8px;
}

.simulation-result-card {
  position:
    relative;

  min-width:
    0;

  padding:
    13px;

  overflow:
    hidden;

  border:
    1px solid
    var(--simulation-border);

  border-radius:
    6px;

  background:
    var(--simulation-card);

  backdrop-filter:
    blur(
      16px
    );
}

.app-shell[
  data-theme="light"
]
.simulation-result-card {
  box-shadow:
    0 7px 22px
    rgba(
      38,
      62,
      72,
      0.055
    );
}

.simulation-result-card::before {
  content:
    "";

  position:
    absolute;

  top:
    0;

  left:
    0;

  width:
    36px;

  height:
    2px;

  background:
    var(--cyan);
}

.simulation-result-card--blue::before {
  background:
    var(--blue);
}

.simulation-result-card--amber::before {
  background:
    var(--amber);
}

.simulation-result-card__icon {
  width:
    30px;

  height:
    30px;

  display:
    grid;

  place-items:
    center;

  margin-bottom:
    14px;

  color:
    var(--cyan);

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.12
    );

  background:
    rgba(
      97,
      216,
      220,
      0.025
    );
}

.app-shell[
  data-theme="light"
]
.simulation-result-card__icon {
  color:
    #118d99;

  border-color:
    rgba(
      17,
      141,
      153,
      0.18
    );

  background:
    rgba(
      17,
      141,
      153,
      0.05
    );
}

.simulation-result-card
> span {
  display:
    block;

  color:
    var(--simulation-muted);

  font-size:
    5px;

  letter-spacing:
    0.13em;
}

.simulation-result-card
> strong {
  display:
    block;

  margin-top:
    7px;

  overflow:
    hidden;

  color:
    var(--simulation-title);

  font-size:
    clamp(
      14px,
      1.6vw,
      21px
    );

  font-weight:
    300;

  text-overflow:
    ellipsis;
}

.app-shell[
  data-theme="light"
]
.simulation-result-card
> strong {
  font-weight:
    500;
}

.simulation-result-card
> small {
  display:
    block;

  margin-top:
    7px;

  color:
    var(--simulation-muted);

  font-size:
    6px;
}

.simulation-view__chart {
  min-height:
    340px;
}

.simulation-view__chart
.analysis-panel {
  height:
    340px;
}

.simulation-comparison {
  display:
    grid;

  grid-template-columns:
    1fr
    120px
    1fr
    auto;

  align-items:
    center;

  gap:
    12px;

  padding:
    14px 16px;

  border:
    1px solid
    var(--simulation-border);

  border-radius:
    6px;

  background:
    var(--simulation-panel);
}

.simulation-comparison span,
.simulation-comparison strong {
  display:
    block;
}

.simulation-comparison
> div
> span {
  color:
    var(--simulation-muted);

  font-size:
    5px;

  letter-spacing:
    0.13em;
}

.simulation-comparison
> div
> strong {
  margin-top:
    4px;

  color:
    var(--simulation-strong);

  font-size:
    9px;
}

.simulation-comparison__connector {
  display:
    flex;

  align-items:
    center;

  gap:
    8px;
}

.simulation-comparison__connector span {
  width:
    100%;

  height:
    1px;

  background:
    rgba(
      97,
      216,
      220,
      0.19
    );
}

.simulation-comparison__connector strong {
  color:
    var(--cyan);

  font-size:
    13px;
}

.simulation-comparison__conclusion {
  padding:
    8px 10px;

  color:
    var(--cyan);

  border:
    1px solid
    rgba(
      97,
      216,
      220,
      0.12
    );

  font-size:
    6px;

  letter-spacing:
    0.1em;

  white-space:
    nowrap;
}

.app-shell[
  data-theme="light"
]
.simulation-comparison__conclusion {
  color:
    #118d99;

  border-color:
    rgba(
      17,
      141,
      153,
      0.18
    );

  background:
    rgba(
      17,
      141,
      153,
      0.04
    );
}

@media (
  max-width:
    1000px
) {
  .simulation-view__workspace {
    grid-template-columns:
      1fr;
  }

  .simulation-results-grid {
    grid-template-columns:
      repeat(
        2,
        1fr
      );
  }
}

@media (
  max-width:
    700px
) {
  .simulation-view {
    padding:
      14px;
  }

  .simulation-view__hero {
    align-items:
      flex-start;

    flex-direction:
      column;
  }

  .simulation-results-grid {
    grid-template-columns:
      1fr;
  }

  .simulation-comparison {
    grid-template-columns:
      1fr;
  }

  .simulation-comparison__connector {
    display:
      none;
  }
}

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\model\ModelView.tsx
============================================================
import {
  ArrowDown,
  ArrowRight,
  CheckCircle2,
  Orbit,
  RadioTower,
  Satellite,
  TrendingDown,
} from 'lucide-react';

import type {
  SimulationInputs,
} from '../../types/simulation';

import './ModelView.css';

interface ModelViewProps {
  inputs: SimulationInputs;
  receivedPower: number;
}

function formatNumber(
  value: number,
  maximumFractionDigits = 4,
) {
  return value.toLocaleString(
    'pt-BR',
    {
      maximumFractionDigits,
    },
  );
}

function ModelView({
  inputs,
  receivedPower,
}: ModelViewProps) {
  const {
    initialPower,
    referenceDistance,
    finalDistance,
    attenuationCoefficient,
  } = inputs;

  const distanceRatio =
    finalDistance !== 0
      ? referenceDistance /
        finalDistance
      : 0;

  const poweredRatio =
    Math.pow(
      distanceRatio,
      attenuationCoefficient,
    );

  const attenuationPercent =
    initialPower > 0
      ? (
          (
            initialPower -
            receivedPower
          ) /
          initialPower
        ) *
        100
      : 0;

  return (
    <section className="model-view">
      <div className="model-view__hero">
        <div className="model-view__hero-content">
          <span className="model-view__eyebrow">
            MODELO MATEMÁTICO
          </span>

          <h1>
            Como a distância afeta
            a potência de um sinal?
          </h1>

          <p>
            Da situação física à solução
            matemática: modelamos o
            decaimento da potência de um
            sinal orbital por meio de uma
            equação diferencial homogênea
            de primeira ordem.
          </p>
        </div>

        <div className="model-view__badge">
          <Orbit size={18} />

          <span>
            EDO HOMOGÊNEA
          </span>
        </div>
      </div>

      <article className="model-card model-card--problem">
        <div className="model-card__heading">
          <span>
            01 · PROBLEMA FÍSICO
          </span>

          <h2>
            Comunicação orbital
          </h2>
        </div>

        <div className="problem-layout">
          <div className="problem-copy">
            <span className="problem-copy__label">
              PROBLEMA PROPOSTO
            </span>

            <p>
              Determinar como a potência
              <strong> P </strong>
              de um sinal de rádio emitido
              por um satélite decresce à
              medida que a distância
              <strong> r </strong>
              até a estação terrestre
              aumenta, considerando que a
              taxa de variação da potência
              por unidade de distância
              <strong> dP/dr </strong>
              depende proporcionalmente da
              relação entre a potência atual
              e a distância percorrida
              <strong> P/r</strong>.
            </p>

            <div className="problem-insight">
              <TrendingDown size={19} />

              <div>
                <span>
                  IDEIA CENTRAL
                </span>

                <strong>
                  Quanto maior a distância,
                  menor tende a ser a
                  potência recebida.
                </strong>
              </div>
            </div>
          </div>

          <div className="scenario-diagram">
            <div className="scenario-object">
              <div className="scenario-object__icon">
                <Satellite size={34} />
              </div>

              <strong>
                Satélite
              </strong>

              <span>
                Transmissor orbital
              </span>
            </div>

            <div className="scenario-link">
              <div className="scenario-link__line" />

              <span>
                sinal · distância r
              </span>
            </div>

            <div className="scenario-object">
              <div className="scenario-object__icon scenario-object__icon--station">
                <RadioTower size={34} />
              </div>

              <strong>
                Estação terrestre
              </strong>

              <span>
                Receptor do sinal
              </span>
            </div>
          </div>
        </div>
      </article>

      <article className="model-card model-card--equation">
        <div className="model-card__heading">
          <span>
            02 · MODELO MATEMÁTICO
          </span>

          <h2>
            Traduzindo o problema em uma EDO
          </h2>
        </div>

        <div className="equation-layout">
          <div className="equation-stage">
            <span className="equation-stage__label">
              EQUAÇÃO DIFERENCIAL
            </span>

            <div className="equation-display equation-display--primary">
              <span className="equation-fraction">
                <span>dP</span>
                <span>dr</span>
              </span>

              <span>=</span>

              <span>−k</span>

              <span className="equation-fraction">
                <span>P</span>
                <span>r</span>
              </span>
            </div>

            <p>
              O sinal negativo indica
              redução da potência à medida
              que a distância aumenta.
            </p>
          </div>

          <div className="equation-meaning">
            <div>
              <strong>
                P
              </strong>

              <span>
                potência atual do sinal
              </span>
            </div>

            <div>
              <strong>
                r
              </strong>

              <span>
                distância analisada
              </span>
            </div>

            <div>
              <strong>
                k
              </strong>

              <span>
                coeficiente de atenuação
              </span>
            </div>

            <div>
              <strong>
                dP/dr
              </strong>

              <span>
                variação da potência
                por unidade de distância
              </span>
            </div>

            <div>
              <strong>
                −
              </strong>

              <span>
                representa o decaimento
                do sinal
              </span>
            </div>

            <div>
              <strong>
                P/r
              </strong>

              <span>
                relação potência-distância
              </span>
            </div>
          </div>
        </div>

        <div className="homogeneous-proof">
          <CheckCircle2 size={19} />

          <div>
            <span>
              POR QUE É HOMOGÊNEA?
            </span>

            <strong>
              O lado direito pode ser
              escrito como uma função da
              razão P/r.
            </strong>
          </div>

          <div className="homogeneous-proof__equation">
            dP/dr = F(P/r)
          </div>
        </div>
      </article>

      <article className="model-card model-card--derivation">
        <div className="model-card__heading">
          <span>
            03 · SOLUÇÃO ANALÍTICA
          </span>

          <h2>
            Resolvendo pela substituição homogênea
          </h2>
        </div>

        <div className="derivation-flow">
          <div className="derivation-step">
            <span className="derivation-step__number">
              1
            </span>

            <div>
              <span className="derivation-step__label">
                SUBSTITUIÇÃO
              </span>

              <div className="derivation-equation">
                P = vr
              </div>
            </div>
          </div>

          <ArrowRight
            className="derivation-arrow derivation-arrow--desktop"
            size={20}
          />

          <ArrowDown
            className="derivation-arrow derivation-arrow--mobile"
            size={20}
          />

          <div className="derivation-step">
            <span className="derivation-step__number">
              2
            </span>

            <div>
              <span className="derivation-step__label">
                DERIVANDO
              </span>

              <div className="derivation-equation">
                dP/dr = v + r(dv/dr)
              </div>
            </div>
          </div>

          <ArrowRight
            className="derivation-arrow derivation-arrow--desktop"
            size={20}
          />

          <ArrowDown
            className="derivation-arrow derivation-arrow--mobile"
            size={20}
          />

          <div className="derivation-step">
            <span className="derivation-step__number">
              3
            </span>

            <div>
              <span className="derivation-step__label">
                SUBSTITUINDO
              </span>

              <div className="derivation-equation">
                v + r(dv/dr) = −kv
              </div>
            </div>
          </div>
        </div>

        <div className="derivation-details">
          <div className="derivation-line">
            <span>
              Reorganizando
            </span>

            <strong>
              r(dv/dr) = −(k + 1)v
            </strong>
          </div>

          <div className="derivation-line">
            <span>
              Separando variáveis
            </span>

            <strong>
              dv/v = −(k + 1) dr/r
            </strong>
          </div>

          <div className="derivation-line">
            <span>
              Integrando
            </span>

            <strong>
              ln|v| = −(k + 1)ln|r| + C
            </strong>
          </div>

          <div className="derivation-line">
            <span>
              Isolando v
            </span>

            <strong>
              v = C r
              <sup>
                −(k + 1)
              </sup>
            </strong>
          </div>

          <div className="derivation-line">
            <span>
              Como P = vr
            </span>

            <strong>
              P = C r
              <sup>
                −k
              </sup>
            </strong>
          </div>
        </div>

        <div className="final-solution">
          <div className="final-solution__copy">
            <span>
              CONSIDERANDO P(r₀) = P₀
            </span>

            <h3>
              Solução analítica final
            </h3>

            <p>
              A potência em qualquer
              distância r pode ser
              calculada diretamente.
            </p>
          </div>

          <div className="final-solution__equation">
            P(r) = P₀
            <span>
              (
              <span className="equation-fraction equation-fraction--inline">
                <span>r₀</span>
                <span>r</span>
              </span>
              )
              <sup>k</sup>
            </span>
          </div>
        </div>
      </article>

      <article className="model-card model-card--example">
        <div className="model-card__heading">
          <span>
            04 · EXEMPLO ATUAL
          </span>

          <h2>
            Resolvendo com os dados da simulação
          </h2>
        </div>

        <div className="example-layout">
          <div className="example-data">
            <span className="example-data__eyebrow">
              DADOS DO PROBLEMA
            </span>

            <div className="numeric-parameters">
              <div>
                <span>
                  P₀
                </span>

                <strong>
                  {formatNumber(
                    initialPower,
                    2,
                  )}
                  {' '}
                  W
                </strong>

                <small>
                  potência inicial
                </small>
              </div>

              <div>
                <span>
                  r₀
                </span>

                <strong>
                  {formatNumber(
                    referenceDistance,
                    2,
                  )}
                  {' '}
                  km
                </strong>

                <small>
                  distância de referência
                </small>
              </div>

              <div>
                <span>
                  r
                </span>

                <strong>
                  {formatNumber(
                    finalDistance,
                    2,
                  )}
                  {' '}
                  km
                </strong>

                <small>
                  distância final
                </small>
              </div>

              <div>
                <span>
                  k
                </span>

                <strong>
                  {formatNumber(
                    attenuationCoefficient,
                    2,
                  )}
                </strong>

                <small>
                  coeficiente
                </small>
              </div>
            </div>
          </div>

          <div className="example-resolution">
            <span className="example-data__eyebrow">
              RESOLUÇÃO PASSO A PASSO
            </span>

            <div className="resolution-step">
              <span>
                1
              </span>

              <div>
                <small>
                  Aplicando a solução
                </small>

                <strong>
                  P(
                  {formatNumber(
                    finalDistance,
                    2,
                  )}
                  ) =
                  {' '}
                  {formatNumber(
                    initialPower,
                    2,
                  )}
                  {' '}
                  (
                  {formatNumber(
                    referenceDistance,
                    2,
                  )}
                  /
                  {formatNumber(
                    finalDistance,
                    2,
                  )}
                  )
                  <sup>
                    {formatNumber(
                      attenuationCoefficient,
                      2,
                    )}
                  </sup>
                </strong>
              </div>
            </div>

            <div className="resolution-step">
              <span>
                2
              </span>

              <div>
                <small>
                  Calculando a razão
                </small>

                <strong>
                  P(
                  {formatNumber(
                    finalDistance,
                    2,
                  )}
                  ) =
                  {' '}
                  {formatNumber(
                    initialPower,
                    2,
                  )}
                  {' '}
                  (
                  {formatNumber(
                    distanceRatio,
                    4,
                  )}
                  )
                  <sup>
                    {formatNumber(
                      attenuationCoefficient,
                      2,
                    )}
                  </sup>
                </strong>
              </div>
            </div>

            <div className="resolution-step">
              <span>
                3
              </span>

              <div>
                <small>
                  Elevando ao expoente k
                </small>

                <strong>
                  P(
                  {formatNumber(
                    finalDistance,
                    2,
                  )}
                  ) =
                  {' '}
                  {formatNumber(
                    initialPower,
                    2,
                  )}
                  {' × '}
                  {formatNumber(
                    poweredRatio,
                    6,
                  )}
                </strong>
              </div>
            </div>
          </div>
        </div>

        <div className="answer-stage">
          <div className="answer-stage__before">
            <span>
              POTÊNCIA INICIAL
            </span>

            <strong>
              {formatNumber(
                initialPower,
                2,
              )}
              {' '}
              W
            </strong>
          </div>

          <div className="answer-stage__loss">
            <TrendingDown size={21} />

            <span>
              {formatNumber(
                attenuationPercent,
                2,
              )}
              %
            </span>

            <small>
              de atenuação
            </small>
          </div>

          <div className="answer-stage__after">
            <span>
              POTÊNCIA RECEBIDA
            </span>

            <strong>
              {formatNumber(
                receivedPower,
                4,
              )}
              {' '}
              W
            </strong>
          </div>
        </div>

        <div className="example-conclusion">
          <RadioTower size={20} />

          <p>
            Para os parâmetros atuais,
            ao passar de
            {' '}
            <strong>
              {formatNumber(
                referenceDistance,
                2,
              )}
              {' '}
              km
            </strong>
            {' '}
            para
            {' '}
            <strong>
              {formatNumber(
                finalDistance,
                2,
              )}
              {' '}
              km
            </strong>
            , a potência calculada do
            sinal é de
            {' '}
            <strong>
              {formatNumber(
                receivedPower,
                4,
              )}
              {' '}
              W
            </strong>
            , correspondendo a
            {' '}
            <strong>
              {formatNumber(
                attenuationPercent,
                2,
              )}
              %
            </strong>
            {' '}
            de atenuação.
          </p>
        </div>
      </article>
    </section>
  );
}

export default ModelView;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\model\ModelView.css
============================================================
.model-view {
  --model-accent:
    #7c6cf2;

  --model-accent-bright:
    #a89cff;

  --model-accent-soft:
    rgba(
      124,
      108,
      242,
      0.09
    );

  --model-accent-border:
    rgba(
      124,
      108,
      242,
      0.25
    );

  --model-gold:
    #e2b761;

  --model-green:
    #68d8b4;

  --model-bg:
    #080b10;

  --model-title:
    #f3f5f6;

  --model-heading:
    #e5eaed;

  --model-text:
    #9aa7ad;

  --model-muted:
    #718087;

  --model-strong:
    #d5dde0;

  --model-border:
    rgba(
      218,
      227,
      232,
      0.09
    );

  --model-divider:
    rgba(
      218,
      227,
      232,
      0.07
    );

  --model-card:
    rgba(
      14,
      19,
      25,
      0.84
    );

  --model-card-elevated:
    rgba(
      19,
      25,
      33,
      0.9
    );

  --model-cell:
    rgba(
      255,
      255,
      255,
      0.027
    );

  --model-cell-soft:
    rgba(
      255,
      255,
      255,
      0.018
    );

  min-height:
    100%;

  overflow-y:
    auto;

  padding:
    clamp(
      18px,
      2.4vw,
      34px
    );

  color:
    var(--model-text);

  background:
    radial-gradient(
      circle at 72% 0%,
      rgba(
        124,
        108,
        242,
        0.11
      ),
      transparent 28%
    ),
    radial-gradient(
      circle at 8% 28%,
      rgba(
        61,
        117,
        154,
        0.06
      ),
      transparent 24%
    ),
    var(--model-bg);
}

.app-shell[
  data-theme="light"
]
.model-view {
  --model-accent:
    #6548d9;

  --model-accent-bright:
    #8069e9;

  --model-accent-soft:
    rgba(
      101,
      72,
      217,
      0.065
    );

  --model-accent-border:
    rgba(
      101,
      72,
      217,
      0.19
    );

  --model-green:
    #168b70;

  --model-bg:
    #f5f6f8;

  --model-title:
    #171a20;

  --model-heading:
    #252a32;

  --model-text:
    #5d6870;

  --model-muted:
    #778188;

  --model-strong:
    #313840;

  --model-border:
    rgba(
      28,
      34,
      42,
      0.09
    );

  --model-divider:
    rgba(
      28,
      34,
      42,
      0.07
    );

  --model-card:
    rgba(
      255,
      255,
      255,
      0.92
    );

  --model-card-elevated:
    #ffffff;

  --model-cell:
    rgba(
      31,
      39,
      49,
      0.025
    );

  --model-cell-soft:
    rgba(
      31,
      39,
      49,
      0.018
    );

  background:
    radial-gradient(
      circle at 72% 0%,
      rgba(
        101,
        72,
        217,
        0.09
      ),
      transparent 30%
    ),
    #f5f6f8;
}

.model-view__hero,
.model-card {
  width:
    min(
      100%,
      1320px
    );

  margin-left:
    auto;

  margin-right:
    auto;
}

.model-view__hero {
  display:
    flex;

  align-items:
    flex-end;

  justify-content:
    space-between;

  gap:
    32px;

  margin-bottom:
    20px;
}

.model-view__hero-content {
  min-width:
    0;
}

.model-view__eyebrow {
  color:
    var(--model-accent-bright);

  font-size:
    11px;

  font-weight:
    800;

  letter-spacing:
    0.18em;
}

.model-view__hero h1 {
  max-width:
    820px;

  margin:
    10px 0 9px;

  color:
    var(--model-title);

  font-size:
    clamp(
      30px,
      4vw,
      50px
    );

  font-weight:
    430;

  line-height:
    1.04;

  letter-spacing:
    -0.045em;
}

.model-view__hero p {
  max-width:
    720px;

  margin:
    0;

  color:
    var(--model-text);

  font-size:
    14px;

  line-height:
    1.65;
}

.model-view__badge {
  flex:
    0 0 auto;

  display:
    flex;

  align-items:
    center;

  gap:
    9px;

  padding:
    11px 14px;

  color:
    var(--model-accent-bright);

  border:
    1px solid
    var(--model-accent-border);

  border-radius:
    999px;

  background:
    var(--model-accent-soft);

  font-size:
    10px;

  font-weight:
    700;

  letter-spacing:
    0.11em;

  white-space:
    nowrap;
}

.model-card {
  overflow:
    hidden;

  margin-bottom:
    14px;

  border:
    1px solid
    var(--model-border);

  border-radius:
    14px;

  background:
    var(--model-card);

  box-shadow:
    0 18px 60px
    rgba(
      0,
      0,
      0,
      0.12
    );

  backdrop-filter:
    blur(
      18px
    );

  -webkit-backdrop-filter:
    blur(
      18px
    );
}

.app-shell[
  data-theme="light"
]
.model-card {
  box-shadow:
    0 12px 34px
    rgba(
      35,
      42,
      55,
      0.055
    );
}

.model-card__heading {
  display:
    flex;

  align-items:
    center;

  justify-content:
    space-between;

  gap:
    24px;

  padding:
    17px 20px;

  border-bottom:
    1px solid
    var(--model-divider);
}

.model-card__heading > span {
  color:
    var(--model-accent-bright);

  font-size:
    10px;

  font-weight:
    800;

  letter-spacing:
    0.15em;
}

.model-card__heading h2 {
  margin:
    0;

  color:
    var(--model-heading);

  font-size:
    14px;

  font-weight:
    600;
}

/* 01 */

.problem-layout {
  display:
    grid;

  grid-template-columns:
    minmax(
      0,
      1.05fr
    )
    minmax(
      360px,
      0.95fr
    );

  gap:
    30px;

  align-items:
    stretch;

  padding:
    28px;
}

.problem-copy {
  display:
    flex;

  flex-direction:
    column;

  justify-content:
    center;

  min-width:
    0;
}

.problem-copy__label,
.example-data__eyebrow {
  color:
    var(--model-muted);

  font-size:
    10px;

  font-weight:
    800;

  letter-spacing:
    0.14em;
}

.problem-copy > p {
  margin:
    12px 0 0;

  color:
    var(--model-strong);

  font-size:
    clamp(
      15px,
      1.5vw,
      19px
    );

  font-weight:
    420;

  line-height:
    1.75;
}

.problem-copy > p strong {
  color:
    var(--model-accent-bright);

  font-family:
    Georgia,
    serif;

  font-weight:
    600;
}

.problem-insight {
  display:
    flex;

  align-items:
    center;

  gap:
    12px;

  margin-top:
    22px;

  padding:
    14px 16px;

  color:
    var(--model-green);

  border:
    1px solid
    rgba(
      104,
      216,
      180,
      0.14
    );

  border-radius:
    10px;

  background:
    rgba(
      104,
      216,
      180,
      0.045
    );
}

.problem-insight span,
.problem-insight strong {
  display:
    block;
}

.problem-insight span {
  color:
    var(--model-muted);

  font-size:
    9px;

  letter-spacing:
    0.14em;
}

.problem-insight strong {
  margin-top:
    3px;

  color:
    var(--model-strong);

  font-size:
    13px;

  font-weight:
    600;
}

.scenario-diagram {
  position:
    relative;

  display:
    grid;

  grid-template-columns:
    1fr 1.35fr 1fr;

  align-items:
    center;

  min-height:
    230px;

  padding:
    24px;

  border:
    1px solid
    var(--model-border);

  border-radius:
    12px;

  background:
    radial-gradient(
      circle at center,
      var(--model-accent-soft),
      transparent 62%
    );
}

.scenario-object {
  display:
    flex;

  flex-direction:
    column;

  align-items:
    center;

  min-width:
    0;

  text-align:
    center;
}

.scenario-object__icon {
  width:
    68px;

  height:
    68px;

  display:
    grid;

  place-items:
    center;

  margin-bottom:
    12px;

  color:
    var(--model-accent-bright);

  border:
    1px solid
    var(--model-accent-border);

  border-radius:
    50%;

  background:
    var(--model-accent-soft);

  box-shadow:
    0 0 34px
    rgba(
      124,
      108,
      242,
      0.09
    );
}

.scenario-object__icon--station {
  color:
    var(--model-gold);

  border-color:
    rgba(
      226,
      183,
      97,
      0.2
    );

  background:
    rgba(
      226,
      183,
      97,
      0.045
    );
}

.scenario-object strong {
  color:
    var(--model-heading);

  font-size:
    13px;
}

.scenario-object span {
  margin-top:
    4px;

  color:
    var(--model-muted);

  font-size:
    10px;
}

.scenario-link {
  min-width:
    0;

  text-align:
    center;
}

.scenario-link__line {
  height:
    1px;

  background:
    linear-gradient(
      90deg,
      rgba(
        124,
        108,
        242,
        0.12
      ),
      var(--model-accent-bright),
      rgba(
        226,
        183,
        97,
        0.4
      )
    );

  box-shadow:
    0 0 14px
    rgba(
      124,
      108,
      242,
      0.2
    );
}

.scenario-link span {
  display:
    inline-block;

  margin-top:
    10px;

  color:
    var(--model-muted);

  font-size:
    10px;

  letter-spacing:
    0.08em;
}

/* 02 */

.equation-layout {
  display:
    grid;

  grid-template-columns:
    minmax(
      300px,
      0.85fr
    )
    minmax(
      0,
      1.15fr
    );

  gap:
    24px;

  padding:
    28px;
}

.equation-stage {
  display:
    flex;

  flex-direction:
    column;

  justify-content:
    center;

  align-items:
    center;

  min-height:
    255px;

  padding:
    22px;

  text-align:
    center;

  border:
    1px solid
    var(--model-accent-border);

  border-radius:
    12px;

  background:
    var(--model-accent-soft);
}

.equation-stage__label {
  color:
    var(--model-muted);

  font-size:
    10px;

  font-weight:
    800;

  letter-spacing:
    0.15em;
}

.equation-stage p {
  max-width:
    350px;

  margin:
    18px 0 0;

  color:
    var(--model-text);

  font-size:
    12px;

  line-height:
    1.55;
}

.equation-display {
  display:
    flex;

  align-items:
    center;

  justify-content:
    center;

  gap:
    14px;

  font-family:
    Georgia,
    serif;
}

.equation-display--primary {
  margin-top:
    22px;

  color:
    var(--model-title);

  font-size:
    clamp(
      30px,
      4vw,
      48px
    );
}

.equation-fraction {
  display:
    inline-flex;

  flex-direction:
    column;

  align-items:
    center;

  vertical-align:
    middle;
}

.equation-fraction > span:first-child {
  padding:
    0 6px 4px;

  border-bottom:
    1px solid
    currentColor;
}

.equation-fraction > span:last-child {
  padding-top:
    4px;
}

.equation-fraction--inline {
  margin:
    0 4px;

  font-size:
    0.88em;
}

.equation-meaning {
  display:
    grid;

  grid-template-columns:
    repeat(
      2,
      minmax(
        0,
        1fr
      )
    );

  gap:
    10px;
}

.equation-meaning > div {
  padding:
    17px;

  border:
    1px solid
    var(--model-border);

  border-radius:
    10px;

  background:
    var(--model-cell);
}

.equation-meaning strong,
.equation-meaning span {
  display:
    block;
}

.equation-meaning strong {
  color:
    var(--model-accent-bright);

  font-family:
    Georgia,
    serif;

  font-size:
    19px;
}

.equation-meaning span {
  margin-top:
    6px;

  color:
    var(--model-text);

  font-size:
    11px;

  line-height:
    1.45;
}

.homogeneous-proof {
  display:
    grid;

  grid-template-columns:
    auto minmax(
      0,
      1fr
    )
    auto;

  align-items:
    center;

  gap:
    12px;

  margin:
    0 28px 28px;

  padding:
    15px 17px;

  color:
    var(--model-green);

  border:
    1px solid
    rgba(
      104,
      216,
      180,
      0.13
    );

  border-radius:
    10px;

  background:
    rgba(
      104,
      216,
      180,
      0.035
    );
}

.homogeneous-proof span,
.homogeneous-proof strong {
  display:
    block;
}

.homogeneous-proof span {
  color:
    var(--model-muted);

  font-size:
    9px;

  letter-spacing:
    0.12em;
}

.homogeneous-proof strong {
  margin-top:
    3px;

  color:
    var(--model-strong);

  font-size:
    12px;

  font-weight:
    500;
}

.homogeneous-proof__equation {
  padding:
    9px 12px;

  color:
    var(--model-accent-bright);

  border:
    1px solid
    var(--model-accent-border);

  border-radius:
    8px;

  font-family:
    Georgia,
    serif;

  font-size:
    16px;

  white-space:
    nowrap;
}

/* 03 */

.derivation-flow {
  display:
    grid;

  grid-template-columns:
    minmax(
      0,
      1fr
    )
    auto
    minmax(
      0,
      1fr
    )
    auto
    minmax(
      0,
      1fr
    );

  align-items:
    center;

  gap:
    14px;

  padding:
    26px 28px 18px;
}

.derivation-step {
  display:
    flex;

  align-items:
    center;

  gap:
    13px;

  min-width:
    0;

  padding:
    16px;

  border:
    1px solid
    var(--model-border);

  border-radius:
    10px;

  background:
    var(--model-cell-soft);
}

.derivation-step__number {
  width:
    34px;

  height:
    34px;

  flex:
    0 0 auto;

  display:
    grid;

  place-items:
    center;

  color:
    white;

  border-radius:
    50%;

  background:
    linear-gradient(
      135deg,
      var(--model-accent),
      var(--model-accent-bright)
    );

  font-size:
    11px;

  font-weight:
    800;

  box-shadow:
    0 0 24px
    rgba(
      124,
      108,
      242,
      0.16
    );
}

.derivation-step__label {
  color:
    var(--model-muted);

  font-size:
    9px;

  font-weight:
    700;

  letter-spacing:
    0.12em;
}

.derivation-equation {
  margin-top:
    7px;

  color:
    var(--model-heading);

  font-family:
    Georgia,
    serif;

  font-size:
    clamp(
      14px,
      1.4vw,
      18px
    );

  overflow-wrap:
    anywhere;
}

.derivation-arrow {
  color:
    var(--model-muted);
}

.derivation-arrow--mobile {
  display:
    none;
}

.derivation-details {
  display:
    grid;

  grid-template-columns:
    repeat(
      5,
      minmax(
        0,
        1fr
      )
    );

  gap:
    8px;

  padding:
    0 28px 24px;
}

.derivation-line {
  min-width:
    0;

  padding:
    14px;

  border:
    1px solid
    var(--model-border);

  border-radius:
    9px;

  background:
    var(--model-cell);
}

.derivation-line span,
.derivation-line strong {
  display:
    block;
}

.derivation-line span {
  color:
    var(--model-muted);

  font-size:
    9px;

  line-height:
    1.35;
}

.derivation-line strong {
  margin-top:
    8px;

  color:
    var(--model-strong);

  font-family:
    Georgia,
    serif;

  font-size:
    12px;

  font-weight:
    400;

  line-height:
    1.5;

  overflow-wrap:
    anywhere;
}

.final-solution {
  display:
    flex;

  align-items:
    center;

  justify-content:
    space-between;

  gap:
    28px;

  margin:
    0 28px 28px;

  padding:
    22px;

  border:
    1px solid
    var(--model-accent-border);

  border-radius:
    12px;

  background:
    linear-gradient(
      135deg,
      var(--model-accent-soft),
      transparent
    );
}

.final-solution__copy span {
  color:
    var(--model-muted);

  font-size:
    9px;

  font-weight:
    700;

  letter-spacing:
    0.12em;
}

.final-solution__copy h3 {
  margin:
    6px 0 0;

  color:
    var(--model-heading);

  font-size:
    15px;
}

.final-solution__copy p {
  max-width:
    360px;

  margin:
    7px 0 0;

  color:
    var(--model-text);

  font-size:
    11px;

  line-height:
    1.5;
}

.final-solution__equation {
  color:
    var(--model-accent-bright);

  font-family:
    Georgia,
    serif;

  font-size:
    clamp(
      24px,
      3vw,
      38px
    );

  white-space:
    nowrap;
}

/* 04 */

.example-layout {
  display:
    grid;

  grid-template-columns:
    minmax(
      0,
      0.9fr
    )
    minmax(
      0,
      1.1fr
    );

  gap:
    24px;

  padding:
    28px;
}

.example-data,
.example-resolution {
  min-width:
    0;
}

.numeric-parameters {
  display:
    grid;

  grid-template-columns:
    repeat(
      2,
      minmax(
        0,
        1fr
      )
    );

  gap:
    10px;

  margin-top:
    13px;
}

.numeric-parameters > div {
  padding:
    17px;

  border:
    1px solid
    var(--model-border);

  border-radius:
    10px;

  background:
    var(--model-cell);
}

.numeric-parameters span,
.numeric-parameters strong,
.numeric-parameters small {
  display:
    block;
}

.numeric-parameters span {
  color:
    var(--model-accent-bright);

  font-family:
    Georgia,
    serif;

  font-size:
    18px;
}

.numeric-parameters strong {
  margin-top:
    6px;

  color:
    var(--model-heading);

  font-size:
    18px;

  font-weight:
    550;
}

.numeric-parameters small {
  margin-top:
    5px;

  color:
    var(--model-muted);

  font-size:
    10px;
}

.example-resolution {
  display:
    flex;

  flex-direction:
    column;
}

.resolution-step {
  display:
    grid;

  grid-template-columns:
    34px
    minmax(
      0,
      1fr
    );

  align-items:
    center;

  gap:
    12px;

  margin-top:
    11px;

  padding:
    14px;

  border:
    1px solid
    var(--model-border);

  border-radius:
    10px;

  background:
    var(--model-cell);
}

.resolution-step > span {
  width:
    34px;

  height:
    34px;

  display:
    grid;

  place-items:
    center;

  color:
    white;

  border-radius:
    50%;

  background:
    var(--model-accent);

  font-size:
    11px;

  font-weight:
    800;
}

.resolution-step small,
.resolution-step strong {
  display:
    block;
}

.resolution-step small {
  color:
    var(--model-muted);

  font-size:
    9px;

  text-transform:
    uppercase;

  letter-spacing:
    0.08em;
}

.resolution-step strong {
  margin-top:
    5px;

  color:
    var(--model-strong);

  font-family:
    Georgia,
    serif;

  font-size:
    15px;

  font-weight:
    400;

  line-height:
    1.45;

  overflow-wrap:
    anywhere;
}

.answer-stage {
  display:
    grid;

  grid-template-columns:
    1fr auto 1fr;

  align-items:
    stretch;

  gap:
    14px;

  margin:
    0 28px 18px;
}

.answer-stage__before,
.answer-stage__after {
  display:
    flex;

  flex-direction:
    column;

  justify-content:
    center;

  padding:
    20px;

  border:
    1px solid
    var(--model-border);

  border-radius:
    12px;

  background:
    var(--model-cell);
}

.answer-stage__before span,
.answer-stage__after span {
  color:
    var(--model-muted);

  font-size:
    9px;

  font-weight:
    700;

  letter-spacing:
    0.12em;
}

.answer-stage__before strong,
.answer-stage__after strong {
  margin-top:
    7px;

  color:
    var(--model-title);

  font-size:
    clamp(
      25px,
      3vw,
      38px
    );

  font-weight:
    450;

  letter-spacing:
    -0.035em;
}

.answer-stage__after {
  border-color:
    var(--model-accent-border);

  background:
    linear-gradient(
      135deg,
      var(--model-accent-soft),
      var(--model-cell)
    );
}

.answer-stage__after strong {
  color:
    var(--model-accent-bright);
}

.answer-stage__loss {
  min-width:
    132px;

  display:
    flex;

  flex-direction:
    column;

  align-items:
    center;

  justify-content:
    center;

  gap:
    4px;

  padding:
    16px;

  color:
    var(--model-gold);

  text-align:
    center;
}

.answer-stage__loss span {
  font-size:
    18px;

  font-weight:
    700;
}

.answer-stage__loss small {
  color:
    var(--model-muted);

  font-size:
    9px;
}

.example-conclusion {
  display:
    flex;

  align-items:
    flex-start;

  gap:
    12px;

  margin:
    0 28px 28px;

  padding:
    16px 18px;

  color:
    var(--model-green);

  border:
    1px solid
    rgba(
      104,
      216,
      180,
      0.14
    );

  border-radius:
    10px;

  background:
    rgba(
      104,
      216,
      180,
      0.04
    );
}

.example-conclusion svg {
  flex:
    0 0 auto;

  margin-top:
    2px;
}

.example-conclusion p {
  margin:
    0;

  color:
    var(--model-text);

  font-size:
    12px;

  line-height:
    1.65;
}

.example-conclusion strong {
  color:
    var(--model-strong);
}

/* TABLET */

@media (
  max-width:
    1000px
) {
  .problem-layout,
  .equation-layout,
  .example-layout {
    grid-template-columns:
      1fr;
  }

  .scenario-diagram {
    min-height:
      210px;
  }

  .derivation-flow {
    grid-template-columns:
      1fr;
  }

  .derivation-arrow--desktop {
    display:
      none;
  }

  .derivation-arrow--mobile {
    display:
      block;

    justify-self:
      center;
  }

  .derivation-details {
    grid-template-columns:
      repeat(
        2,
        minmax(
          0,
          1fr
        )
      );
  }

  .derivation-line:last-child {
    grid-column:
      1 / -1;
  }
}

/* MOBILE */

@media (
  max-width:
    680px
) {
  .model-view {
    padding:
      16px 12px 28px;
  }

  .model-view__hero {
    align-items:
      flex-start;

    flex-direction:
      column;

    gap:
      16px;

    margin-bottom:
      16px;
  }

  .model-view__hero h1 {
    margin-top:
      8px;

    font-size:
      clamp(
        30px,
        10vw,
        40px
      );

    line-height:
      1.06;
  }

  .model-view__hero p {
    font-size:
      13px;
  }

  .model-view__badge {
    padding:
      9px 12px;
  }

  .model-card {
    margin-bottom:
      12px;

    border-radius:
      12px;
  }

  .model-card__heading {
    align-items:
      flex-start;

    flex-direction:
      column;

    gap:
      5px;

    padding:
      15px 16px;
  }

  .model-card__heading > span {
    font-size:
      9px;
  }

  .model-card__heading h2 {
    font-size:
      15px;
  }

  .problem-layout,
  .equation-layout,
  .example-layout {
    gap:
      20px;

    padding:
      20px 16px;
  }

  .problem-copy > p {
    font-size:
      15px;

    line-height:
      1.7;
  }

  .problem-insight {
    align-items:
      flex-start;

    padding:
      13px;
  }

  .scenario-diagram {
    grid-template-columns:
      1fr;

    gap:
      14px;

    min-height:
      auto;

    padding:
      20px;
  }

  .scenario-link {
    width:
      100%;
  }

  .scenario-link__line {
    width:
      1px;

    height:
      46px;

    margin:
      0 auto;

    background:
      linear-gradient(
        180deg,
        var(--model-accent-bright),
        var(--model-gold)
      );
  }

  .scenario-link span {
    margin-top:
      7px;
  }

  .equation-stage {
    min-height:
      210px;

    padding:
      20px 14px;
  }

  .equation-display--primary {
    gap:
      10px;

    font-size:
      clamp(
        28px,
        10vw,
        38px
      );
  }

  .equation-meaning {
    grid-template-columns:
      1fr;
  }

  .equation-meaning > div {
    padding:
      14px;
  }

  .homogeneous-proof {
    grid-template-columns:
      auto minmax(
        0,
        1fr
      );

    margin:
      0 16px 20px;

    padding:
      14px;
  }

  .homogeneous-proof__equation {
    grid-column:
      1 / -1;

    width:
      100%;

    text-align:
      center;
  }

  .derivation-flow {
    gap:
      8px;

    padding:
      20px 16px 12px;
  }

  .derivation-step {
    width:
      100%;

    padding:
      14px;
  }

  .derivation-equation {
    font-size:
      15px;
  }

  .derivation-details {
    grid-template-columns:
      1fr;

    gap:
      8px;

    padding:
      0 16px 18px;
  }

  .derivation-line:last-child {
    grid-column:
      auto;
  }

  .derivation-line {
    padding:
      13px;
  }

  .derivation-line strong {
    font-size:
      13px;
  }

  .final-solution {
    align-items:
      flex-start;

    flex-direction:
      column;

    gap:
      18px;

    margin:
      0 16px 20px;

    padding:
      18px;
  }

  .final-solution__equation {
    width:
      100%;

    font-size:
      clamp(
        22px,
        7.4vw,
        30px
      );

    text-align:
      center;

    white-space:
      normal;
  }

  .numeric-parameters {
    grid-template-columns:
      1fr 1fr;
  }

  .numeric-parameters > div {
    padding:
      14px;
  }

  .numeric-parameters strong {
    font-size:
      16px;
  }

  .resolution-step {
    grid-template-columns:
      30px
      minmax(
        0,
        1fr
      );

    gap:
      10px;

    padding:
      13px;
  }

  .resolution-step > span {
    width:
      30px;

    height:
      30px;
  }

  .resolution-step strong {
    font-size:
      14px;
  }

  .answer-stage {
    grid-template-columns:
      1fr;

    gap:
      8px;

    margin:
      0 16px 16px;
  }

  .answer-stage__before,
  .answer-stage__after {
    padding:
      18px;
  }

  .answer-stage__loss {
    min-width:
      0;

    flex-direction:
      row;

    padding:
      10px;
  }

  .answer-stage__loss small {
    margin-left:
      2px;
  }

  .example-conclusion {
    margin:
      0 16px 20px;

    padding:
      14px;
  }

  .example-conclusion p {
    font-size:
      12px;
  }
}

/* CELULARES PEQUENOS */

@media (
  max-width:
    420px
) {
  .model-view {
    padding-left:
      10px;

    padding-right:
      10px;
  }

  .model-view__hero h1 {
    font-size:
      29px;
  }

  .model-view__eyebrow {
    font-size:
      9px;
  }

  .model-view__badge {
    font-size:
      9px;
  }

  .problem-layout,
  .equation-layout,
  .example-layout {
    padding-left:
      14px;

    padding-right:
      14px;
  }

  .numeric-parameters {
    grid-template-columns:
      1fr;
  }

  .equation-display--primary {
    font-size:
      27px;
  }

  .final-solution__equation {
    font-size:
      22px;
  }

  .answer-stage__before strong,
  .answer-stage__after strong {
    font-size:
      27px;
  }
}
