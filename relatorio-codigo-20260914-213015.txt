
============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\layout\Dashboard.tsx
============================================================

import { useState } from 'react';

import {
  Activity,
  Gauge,
  Radio,
  Ruler,
} from 'lucide-react';

import AnalysisPreview from '../charts/AnalysisPreview';
import GlobeScene from '../globe/GlobeScene';
import ControlPanel from '../simulation/ControlPanel';
import MetricCard from '../simulation/MetricCard';
import SatelliteTelemetry from '../simulation/SatelliteTelemetry';
import Header from './Header';

import type {
  SimulationField,
  SimulationInputs,
} from '../../types/simulation';

interface DashboardProps {
  inputs: SimulationInputs;

  onInputChange: (
    field: SimulationField,
    value: number,
  ) => void;

  onReset: () => void;
}

function Dashboard({
  inputs,
  onInputChange,
  onReset,
}: DashboardProps) {
  const [simulationActive, setSimulationActive] =
    useState(false);

  function handleSimulate() {
    setSimulationActive(true);
  }

  function handleReset() {
    setSimulationActive(false);
    onReset();
  }

  return (
    <div className="app-shell">
      <Header />

      <main className="dashboard">
        <div className="dashboard__upper">
          <ControlPanel
            inputs={inputs}
            onChange={onInputChange}
            onSimulate={handleSimulate}
            onReset={handleReset}
          />

          <GlobeScene
            inputs={inputs}
            simulationActive={simulationActive}
          />

          <SatelliteTelemetry
            inputs={inputs}
            simulationActive={simulationActive}
          />
        </div>

        <div className="dashboard__lower">
          <section className="metrics-section">
            <div className="section-heading">
              <div>
                <span className="panel__eyebrow">
                  SIMULATION DATA
                </span>

                <h2>Input Metrics</h2>
              </div>

              <span className="section-heading__status">
                {simulationActive
                  ? 'Simulation armed'
                  : 'Waiting for simulation'}
              </span>
            </div>

            <div className="metrics-grid">
              <MetricCard
                label="INITIAL POWER"
                value={inputs.initialPower.toLocaleString()}
                unit="W"
                description="Transmitted signal power"
                icon={
                  <Radio
                    size={17}
                    strokeWidth={1.6}
                  />
                }
              />

              <MetricCard
                label="DISTANCE"
                value={inputs.finalDistance.toLocaleString()}
                unit="km"
                description="Satellite-to-station range"
                icon={
                  <Ruler
                    size={17}
                    strokeWidth={1.6}
                  />
                }
                accent="blue"
              />

              <MetricCard
                label="COEFFICIENT"
                value={inputs.attenuationCoefficient.toFixed(
                  2,
                )}
                description="Homogeneous EDO parameter k"
                icon={
                  <Gauge
                    size={17}
                    strokeWidth={1.6}
                  />
                }
                accent="amber"
              />

              <MetricCard
                label="NUMERICAL STEP"
                value={inputs.numericalStep.toLocaleString()}
                unit="km"
                description="RK4 integration interval"
                icon={
                  <Activity
                    size={17}
                    strokeWidth={1.6}
                  />
                }
              />
            </div>
          </section>

          <AnalysisPreview />
        </div>
      </main>
    </div>
  );
}

export default Dashboard;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\globe\GlobeScene.tsx
============================================================

import {
  Play,
  RotateCcw,
  SlidersHorizontal,
} from 'lucide-react';

import type {
  SimulationField,
  SimulationInputs,
} from '../../types/simulation';

interface ControlPanelProps {
  inputs: SimulationInputs;

  onChange: (
    field: SimulationField,
    value: number,
  ) => void;

  onSimulate: () => void;
  onReset: () => void;
}

interface NumberFieldProps {
  label: string;
  symbol: string;
  unit?: string;
  value: number;
  step?: number;
  min?: number;

  onChange: (value: number) => void;
}

function NumberField({
  label,
  symbol,
  unit,
  value,
  step = 1,
  min = 0,
  onChange,
}: NumberFieldProps) {
  return (
    <label className="control-field">
      <div className="control-field__header">
        <span>{label}</span>

        <span className="control-field__symbol">
          {symbol}
        </span>
      </div>

      <div className="control-field__input-wrapper">
        <input
          className="control-field__input"
          type="number"
          min={min}
          step={step}
          value={value}
          onChange={(event) =>
            onChange(Number(event.target.value))
          }
        />

        {unit && (
          <span className="control-field__unit">
            {unit}
          </span>
        )}
      </div>
    </label>
  );
}

function ControlPanel({
  inputs,
  onChange,
  onSimulate,
  onReset,
}: ControlPanelProps) {
  return (
    <aside className="panel control-panel">
      <div className="panel__heading">
        <div>
          <span className="panel__eyebrow">
            MISSION INPUT
          </span>

          <h2>Simulation Parameters</h2>
        </div>

        <SlidersHorizontal
          size={18}
          strokeWidth={1.5}
        />
      </div>

      <div className="control-panel__fields">
        <NumberField
          label="Initial power"
          symbol="P₀"
          unit="W"
          value={inputs.initialPower}
          min={0.1}
          step={1}
          onChange={(value) =>
            onChange('initialPower', value)
          }
        />

        <NumberField
          label="Reference distance"
          symbol="r₀"
          unit="km"
          value={inputs.referenceDistance}
          min={1}
          step={10}
          onChange={(value) =>
            onChange('referenceDistance', value)
          }
        />

        <NumberField
          label="Final distance"
          symbol="r"
          unit="km"
          value={inputs.finalDistance}
          min={1}
          step={100}
          onChange={(value) =>
            onChange('finalDistance', value)
          }
        />

        <NumberField
          label="Attenuation coefficient"
          symbol="k"
          value={inputs.attenuationCoefficient}
          min={0.1}
          step={0.1}
          onChange={(value) =>
            onChange(
              'attenuationCoefficient',
              value,
            )
          }
        />

        <NumberField
          label="Numerical step"
          symbol="Δr"
          unit="km"
          value={inputs.numericalStep}
          min={1}
          step={1}
          onChange={(value) =>
            onChange('numericalStep', value)
          }
        />
      </div>

      <div className="control-panel__actions">
        <button
          className="button button--primary"
          type="button"
          onClick={onSimulate}
        >
          <Play size={15} fill="currentColor" />

          Run simulation
        </button>

        <button
          className="button button--ghost"
          type="button"
          onClick={onReset}
          title="Reset parameters"
        >
          <RotateCcw size={16} />
        </button>
      </div>

      <div className="control-panel__footer">
        <span className="status-dot status-dot--cyan" />

        MODEL READY
      </div>
    </aside>
  );
}

export default ControlPanel;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\components\charts\AnalysisPreview.tsx
============================================================

import {
  Activity,
  Box,
} from 'lucide-react';

function AnalysisPreview() {
  return (
    <section className="analysis-panel">
      <div className="analysis-panel__header">
        <div>
          <span className="panel__eyebrow">
            EDO ANALYSIS
          </span>

          <h2>3D Phase Space</h2>
        </div>

        <div className="analysis-panel__mode">
          <Box size={15} />

          X · Y · Z
        </div>
      </div>

      <div className="analysis-preview">
        <div className="analysis-preview__grid" />

        <div className="analysis-preview__axis analysis-preview__axis--x">
          <span>r</span>
        </div>

        <div className="analysis-preview__axis analysis-preview__axis--y">
          <span>P(r)</span>
        </div>

        <div className="analysis-preview__axis analysis-preview__axis--z">
          <span>dP/dr</span>
        </div>

        <svg
          className="analysis-preview__curve"
          viewBox="0 0 600 260"
          preserveAspectRatio="none"
          aria-hidden="true"
        >
          <defs>
            <linearGradient
              id="curveGradient"
              x1="0"
              y1="0"
              x2="1"
              y2="0"
            >
              <stop
                offset="0%"
                stopColor="#67e8f9"
              />

              <stop
                offset="100%"
                stopColor="#3b82f6"
              />
            </linearGradient>
          </defs>

          <path
            d="
              M 40 35
              C 90 62, 110 88, 150 112
              C 205 147, 240 162, 290 178
              C 355 198, 430 208, 560 220
            "
            fill="none"
            stroke="url(#curveGradient)"
            strokeWidth="3"
            strokeLinecap="round"
          />
        </svg>

        <div className="analysis-preview__point analysis-preview__point--1" />
        <div className="analysis-preview__point analysis-preview__point--2" />
        <div className="analysis-preview__point analysis-preview__point--3" />

        <div className="analysis-preview__message">
          <Activity size={17} />

          <div>
            <strong>
              Interactive EDO trajectory
            </strong>

            <span>
              Analytical × numerical solution
            </span>
          </div>
        </div>
      </div>

      <div className="analysis-panel__legend">
        <div>
          <span className="analysis-legend analysis-legend--analytical" />

          Analytical
        </div>

        <div>
          <span className="analysis-legend analysis-legend--numeric" />

          RK4 numerical
        </div>
      </div>
    </section>
  );
}

export default AnalysisPreview;

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\src\App.css
============================================================

#root {
  min-height: 100vh;
}

.app-shell {
  min-height: 100vh;
  background:
    linear-gradient(
      rgba(255, 255, 255, 0.012) 1px,
      transparent 1px
    ),
    linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.012) 1px,
      transparent 1px
    );

  background-size: 42px 42px;
}

/* HEADER */

.topbar {
  height: 72px;
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  padding: 0 26px;

  border-bottom: 1px solid var(--border);
  background: rgba(5, 14, 18, 0.84);
  backdrop-filter: blur(18px);

  position: sticky;
  top: 0;
  z-index: 100;
}

.brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.brand__icon {
  width: 38px;
  height: 38px;

  display: grid;
  place-items: center;

  border: 1px solid rgba(104, 228, 231, 0.2);
  background: rgba(104, 228, 231, 0.06);
  color: var(--cyan);
}

.brand__name {
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.16em;
}

.brand__subtitle {
  margin-top: 3px;
  font-size: 10px;
  color: var(--muted);
  letter-spacing: 0.04em;
}

.topbar__navigation {
  display: flex;
  align-items: center;
  gap: 8px;
}

.topbar__navigation-item {
  position: relative;
  padding: 27px 15px 25px;

  cursor: pointer;

  color: var(--muted);
  background: transparent;

  text-transform: uppercase;
  font-size: 10px;
  font-weight: 650;
  letter-spacing: 0.13em;

  transition:
    color 180ms ease,
    background 180ms ease;
}

.topbar__navigation-item:hover {
  color: var(--text);
}

.topbar__navigation-item--active {
  color: var(--cyan);
}

.topbar__navigation-item--active::after {
  content: "";

  position: absolute;
  left: 14px;
  right: 14px;
  bottom: 0;

  height: 2px;

  background: var(--cyan);

  box-shadow:
    0 0 12px rgba(104, 228, 231, 0.8);
}

.system-status {
  justify-self: end;

  display: flex;
  align-items: center;
  gap: 10px;

  color: var(--muted-light);
}

.system-status__signal {
  width: 28px;
  height: 28px;

  display: grid;
  place-items: center;

  color: var(--cyan);

  border: 1px solid rgba(104, 228, 231, 0.16);
}

.system-status__label,
.system-status__value {
  display: block;
}

.system-status__label {
  font-size: 8px;
  letter-spacing: 0.16em;
  color: var(--muted);
}

.system-status__value {
  margin-top: 2px;

  color: var(--cyan);

  font-size: 9px;
  letter-spacing: 0.16em;
}

.system-status__satellite {
  margin-left: 6px;
  color: #b4c1c6;
}

/* GENERAL */

.dashboard {
  width: 100%;
  max-width: 1800px;

  margin: 0 auto;
  padding: 16px;
}

.dashboard__upper {
  display: grid;

  grid-template-columns:
    minmax(245px, 0.8fr)
    minmax(480px, 2.1fr)
    minmax(245px, 0.8fr);

  gap: 12px;
}

.dashboard__lower {
  display: grid;
  grid-template-columns: minmax(540px, 1.55fr) minmax(430px, 1fr);

  gap: 12px;

  margin-top: 12px;
}

.panel,
.globe-panel,
.metrics-section,
.analysis-panel {
  border: 1px solid var(--border);
  background:
    linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.018),
      transparent 38%
    ),
    rgba(9, 20, 25, 0.82);

  overflow: hidden;
}

.panel__heading,
.section-heading,
.analysis-panel__header,
.globe-panel__header {
  min-height: 68px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  padding: 15px 17px;

  border-bottom: 1px solid var(--border);
}

.panel__heading h2,
.section-heading h2,
.analysis-panel__header h2,
.globe-panel__header h2 {
  margin: 4px 0 0;

  font-size: 13px;
  font-weight: 600;

  letter-spacing: 0.015em;
}

.panel__eyebrow {
  display: block;

  color: var(--muted);

  font-size: 8px;
  font-weight: 650;
  letter-spacing: 0.18em;
}

/* CONTROLS */

.control-panel {
  min-height: 535px;

  display: flex;
  flex-direction: column;
}

.control-panel__fields {
  display: flex;
  flex-direction: column;
  gap: 17px;

  padding: 19px 17px;
}

.control-field {
  display: block;
}

.control-field__header {
  margin-bottom: 8px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  color: #a7b5ba;

  font-size: 10px;
}

.control-field__symbol {
  color: var(--cyan);

  font-family: "Times New Roman", serif;
  font-size: 13px;
}

.control-field__input-wrapper {
  height: 42px;

  display: flex;
  align-items: center;

  border: 1px solid var(--border-strong);

  background: rgba(4, 12, 15, 0.62);

  transition:
    border-color 180ms ease,
    box-shadow 180ms ease;
}

.control-field__input-wrapper:focus-within {
  border-color: rgba(104, 228, 231, 0.42);

  box-shadow:
    0 0 0 1px rgba(104, 228, 231, 0.05),
    inset 0 0 20px rgba(104, 228, 231, 0.025);
}

.control-field__input {
  width: 100%;
  height: 100%;

  border: 0;

  padding: 0 12px;

  color: var(--text);
  background: transparent;

  font-size: 12px;
}

.control-field__unit {
  padding-right: 12px;

  color: var(--muted);

  font-size: 9px;
  letter-spacing: 0.08em;
}

.control-panel__actions {
  display: flex;
  gap: 8px;

  padding: 0 17px 17px;
}

.button {
  height: 41px;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  gap: 8px;

  cursor: pointer;

  text-transform: uppercase;

  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.13em;

  transition:
    transform 160ms ease,
    background 160ms ease,
    border-color 160ms ease;
}

.button:active {
  transform: translateY(1px);
}

.button--primary {
  flex: 1;

  color: #051113;
  background: var(--cyan);
}

.button--primary:hover {
  background: #83eef0;
}

.button--ghost {
  width: 41px;

  color: #a7b6bc;
  background: transparent;

  border: 1px solid var(--border-strong);
}

.button--ghost:hover {
  border-color: rgba(104, 228, 231, 0.3);
  color: var(--cyan);
}

.control-panel__footer {
  margin-top: auto;

  height: 42px;

  display: flex;
  align-items: center;
  gap: 8px;

  padding: 0 17px;

  color: var(--muted);

  border-top: 1px solid var(--border);

  font-size: 8px;
  letter-spacing: 0.15em;
}

/* STATUS */

.status-dot {
  width: 6px;
  height: 6px;

  display: inline-block;

  border-radius: 50%;

  background: #52636a;

  box-shadow:
    0 0 0 3px rgba(82, 99, 106, 0.09);
}

.status-dot--cyan {
  background: var(--cyan);

  box-shadow:
    0 0 8px rgba(104, 228, 231, 0.9),
    0 0 0 3px rgba(104, 228, 231, 0.09);
}

/* GLOBE */

.globe-panel {
  min-height: 535px;
}

.globe-panel__coordinates {
  color: var(--muted);

  font-size: 8px;
  letter-spacing: 0.14em;
}

.globe-stage {
  position: relative;

  min-height: 415px;

  display: flex;
  align-items: center;
  justify-content: center;

  overflow: hidden;

  background:
    radial-gradient(
      circle at center,
      rgba(51, 150, 176, 0.08),
      transparent 44%
    ),
    linear-gradient(
      rgba(255, 255, 255, 0.018) 1px,
      transparent 1px
    ),
    linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.018) 1px,
      transparent 1px
    );

  background-size:
    auto,
    42px 42px,
    42px 42px;
}

.globe-stage::after {
  content: "";

  position: absolute;
  inset: 0;

  pointer-events: none;

  background:
    linear-gradient(
      to bottom,
      rgba(7, 16, 20, 0.4),
      transparent 20%,
      transparent 80%,
      rgba(7, 16, 20, 0.45)
    );
}

.earth {
  position: relative;
  z-index: 4;

  width: min(300px, 44vw);
  aspect-ratio: 1;

  overflow: hidden;

  border-radius: 50%;

  border: 1px solid rgba(112, 214, 235, 0.28);

  background:
    radial-gradient(
      circle at 35% 32%,
      rgba(72, 172, 191, 0.4),
      transparent 19%
    ),
    radial-gradient(
      circle at 65% 69%,
      rgba(50, 116, 130, 0.38),
      transparent 22%
    ),
    radial-gradient(
      circle at 35% 30%,
      #173e4b 0%,
      #0d2933 42%,
      #07151a 72%
    );

  box-shadow:
    -22px 2px 45px rgba(0, 0, 0, 0.52),
    12px -8px 38px rgba(58, 185, 211, 0.08),
    0 0 40px rgba(71, 186, 211, 0.1);
}

.earth::before {
  content: "";

  position: absolute;

  width: 38%;
  height: 27%;

  left: 18%;
  top: 24%;

  border-radius:
    56% 44% 65% 35% /
    48% 55% 45% 52%;

  transform: rotate(-15deg);

  background:
    linear-gradient(
      130deg,
      rgba(103, 155, 140, 0.85),
      rgba(49, 89, 85, 0.42)
    );

  filter: blur(0.2px);

  box-shadow:
    87px 73px 0 -18px rgba(81, 131, 119, 0.55),
    122px -21px 0 -28px rgba(86, 139, 125, 0.48);
}

.earth::after {
  content: "";

  position: absolute;
  inset: 0;

  border-radius: 50%;

  box-shadow:
    inset -63px -14px 85px rgba(0, 0, 0, 0.78),
    inset 18px 0 42px rgba(102, 215, 232, 0.08);
}

.earth__grid {
  position: absolute;
  inset: 0;
  z-index: 3;

  pointer-events: none;

  opacity: 0.15;
}

.earth__grid--vertical {
  background:
    repeating-radial-gradient(
      ellipse at center,
      transparent 0 18px,
      rgba(111, 227, 233, 0.34) 19px 20px
    );

  transform: scaleX(0.54);
}

.earth__grid--horizontal {
  background:
    repeating-linear-gradient(
      to bottom,
      transparent 0 29px,
      rgba(111, 227, 233, 0.18) 30px 31px
    );

  mask-image:
    radial-gradient(
      circle,
      black 0%,
      black 62%,
      transparent 77%
    );
}

.ground-station {
  position: absolute;
  z-index: 9;

  bottom: 29%;
  left: 53%;

  display: flex;
  flex-direction: column;
  align-items: center;

  color: var(--cyan);

  filter:
    drop-shadow(
      0 0 5px rgba(104, 228, 231, 0.65)
    );
}

.ground-station span {
  margin-top: 4px;

  white-space: nowrap;

  color: rgba(197, 236, 240, 0.68);

  font-size: 6px;
  letter-spacing: 0.12em;
}

.orbit {
  position: absolute;
  z-index: 3;

  border: 1px solid rgba(122, 205, 220, 0.18);

  border-radius: 50%;

  pointer-events: none;
}

.orbit--outer {
  width: 520px;
  height: 245px;

  transform: rotate(-16deg);
}

.orbit--inner {
  width: 395px;
  height: 360px;

  opacity: 0.6;

  transform:
    rotate(49deg)
    scaleY(0.4);
}

.satellite-node {
  position: absolute;
  z-index: 10;

  top: 17%;
  right: 21%;

  display: flex;
  align-items: center;
  gap: 7px;

  color: #a8bec5;

  transition:
    color 240ms ease,
    filter 240ms ease;
}

.satellite-node span {
  font-size: 7px;
  letter-spacing: 0.12em;
}

.satellite-node--active {
  color: var(--cyan);

  filter:
    drop-shadow(
      0 0 7px rgba(104, 228, 231, 0.65)
    );
}

.signal-beam {
  position: absolute;
  z-index: 6;

  top: 29%;
  left: 57%;

  width: 2px;
  height: 150px;

  opacity: 0.2;

  transform:
    rotate(34deg)
    translateX(-50%);

  transform-origin: top center;

  background:
    linear-gradient(
      to bottom,
      var(--cyan),
      rgba(104, 228, 231, 0.1)
    );

  box-shadow:
    0 0 8px rgba(104, 228, 231, 0.3);

  transition:
    opacity 240ms ease,
    box-shadow 240ms ease;
}

.signal-beam--active {
  opacity: 0.9;

  box-shadow:
    0 0 12px rgba(104, 228, 231, 0.8),
    0 0 25px rgba(104, 228, 231, 0.15);
}

.globe-data {
  position: absolute;
  z-index: 15;

  color: var(--muted);

  font-size: 7px;
  letter-spacing: 0.12em;
}

.globe-data span,
.globe-data strong {
  display: block;
}

.globe-data strong {
  margin-top: 5px;

  color: #bbcbcf;

  font-size: 10px;
  font-weight: 600;
}

.globe-data--left {
  left: 24px;
  bottom: 29px;
}

.globe-data--right {
  right: 24px;
  top: 29px;

  text-align: right;
}

.globe-axis {
  position: absolute;

  background: rgba(104, 228, 231, 0.04);
}

.globe-axis--x {
  width: 70%;
  height: 1px;
}

.globe-axis--y {
  width: 1px;
  height: 72%;
}

.star {
  position: absolute;

  width: 2px;
  height: 2px;

  background: #bdcbd0;

  border-radius: 50%;

  opacity: 0.55;
}

.star--1 {
  top: 14%;
  left: 16%;
}

.star--2 {
  top: 30%;
  left: 79%;
}

.star--3 {
  top: 77%;
  left: 17%;
}

.star--4 {
  top: 72%;
  right: 17%;
}

.star--5 {
  top: 11%;
  left: 54%;
}

.globe-panel__footer {
  min-height: 50px;

  display: flex;
  align-items: center;
  justify-content: center;
  gap: 28px;

  padding: 10px 18px;

  color: var(--muted);

  border-top: 1px solid var(--border);

  font-size: 8px;
}

.globe-panel__footer > div {
  display: flex;
  align-items: center;
  gap: 6px;
}

.legend-dot {
  width: 5px;
  height: 5px;

  border-radius: 50%;
}

.legend-dot--satellite {
  background: var(--cyan);
}

.legend-dot--station {
  background: #b8c6ca;
}

.legend-line {
  width: 15px;
  height: 1px;

  background: var(--cyan);
}

/* TELEMETRY */

.telemetry-panel {
  min-height: 535px;
}

.satellite-identity {
  display: flex;
  align-items: center;

  gap: 12px;

  padding: 19px 17px;

  border-bottom: 1px solid var(--border);
}

.satellite-identity__icon {
  width: 51px;
  height: 51px;

  display: grid;
  place-items: center;

  color: var(--cyan);

  border: 1px solid rgba(104, 228, 231, 0.14);

  background:
    radial-gradient(
      circle,
      rgba(104, 228, 231, 0.1),
      transparent 68%
    );
}

.satellite-identity__code {
  display: block;

  margin-bottom: 5px;

  color: var(--cyan);

  font-size: 8px;
  letter-spacing: 0.16em;
}

.satellite-identity strong {
  font-size: 10px;
  font-weight: 550;

  color: #c0ccd0;
}

.telemetry-status {
  height: 38px;

  display: flex;
  align-items: center;
  gap: 8px;

  padding: 0 17px;

  color: var(--muted);

  border-bottom: 1px solid var(--border);

  font-size: 8px;
  letter-spacing: 0.13em;
}

.telemetry-list {
  padding: 8px 17px;
}

.telemetry-row {
  min-height: 63px;

  display: flex;
  align-items: center;
  gap: 11px;

  border-bottom: 1px solid var(--border);
}

.telemetry-row:last-child {
  border-bottom: 0;
}

.telemetry-row__icon {
  width: 31px;
  height: 31px;

  display: grid;
  place-items: center;

  color: #8ea0a7;

  border: 1px solid var(--border);
}

.telemetry-row span,
.telemetry-row strong {
  display: block;
}

.telemetry-row span {
  color: var(--muted);

  font-size: 8px;
}

.telemetry-row strong {
  margin-top: 4px;

  color: #becbce;

  font-size: 10px;
  font-weight: 600;
}

.telemetry-panel__footer {
  margin: 8px 17px 17px;

  padding: 13px 12px;

  border: 1px solid rgba(104, 228, 231, 0.12);

  background: rgba(104, 228, 231, 0.025);
}

.telemetry-panel__footer span,
.telemetry-panel__footer strong {
  display: block;
}

.telemetry-panel__footer span {
  color: var(--muted);

  font-size: 7px;
  letter-spacing: 0.14em;
}

.telemetry-panel__footer strong {
  margin-top: 7px;

  color: var(--cyan);

  font-family: Georgia, serif;
  font-size: 13px;
  font-weight: 400;
}

/* METRICS */

.metrics-section {
  min-height: 260px;
}

.section-heading__status {
  color: var(--muted);

  font-size: 8px;
  letter-spacing: 0.11em;
  text-transform: uppercase;
}

.metrics-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);

  gap: 1px;

  background: var(--border);
}

.metric-card {
  position: relative;

  min-height: 190px;

  padding: 18px;

  background: #0a151a;
}

.metric-card::before {
  content: "";

  position: absolute;
  top: 0;
  left: 0;

  width: 36px;
  height: 1px;

  background: var(--cyan);
}

.metric-card--blue::before {
  background: var(--blue);
}

.metric-card--amber::before {
  background: var(--amber);
}

.metric-card__top {
  display: flex;
  align-items: center;
  justify-content: space-between;

  color: var(--muted);

  font-size: 7px;
  letter-spacing: 0.15em;
}

.metric-card__icon {
  color: #75878f;
}

.metric-card__value {
  margin-top: 38px;

  color: #e4ecee;

  font-size: clamp(25px, 2vw, 35px);
  font-weight: 300;
  letter-spacing: -0.04em;
}

.metric-card__value span {
  margin-left: 6px;

  color: var(--muted);

  font-size: 9px;
  letter-spacing: 0.05em;
}

.metric-card__description {
  margin-top: 17px;

  color: #66787f;

  font-size: 8px;
}

/* ANALYSIS */

.analysis-panel {
  min-height: 260px;
}

.analysis-panel__mode {
  display: flex;
  align-items: center;
  gap: 6px;

  color: var(--muted);

  font-size: 8px;
  letter-spacing: 0.13em;
}

.analysis-preview {
  position: relative;

  min-height: 168px;

  overflow: hidden;

  background:
    radial-gradient(
      circle at center,
      rgba(71, 163, 185, 0.055),
      transparent 57%
    );
}

.analysis-preview__grid {
  position: absolute;
  inset: 0;

  background:
    linear-gradient(
      rgba(255, 255, 255, 0.025) 1px,
      transparent 1px
    ),
    linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.025) 1px,
      transparent 1px
    );

  background-size: 28px 28px;

  transform:
    perspective(300px)
    rotateX(52deg)
    scale(1.4);

  transform-origin: center bottom;
}

.analysis-preview__curve {
  position: absolute;

  left: 7%;
  right: 7%;
  bottom: 13px;

  width: 86%;
  height: 83%;
}

.analysis-preview__point {
  position: absolute;

  width: 6px;
  height: 6px;

  border: 1px solid var(--cyan);
  border-radius: 50%;

  background: #092027;

  box-shadow:
    0 0 8px rgba(104, 228, 231, 0.65);
}

.analysis-preview__point--1 {
  left: 21%;
  top: 38%;
}

.analysis-preview__point--2 {
  left: 49%;
  top: 62%;
}

.analysis-preview__point--3 {
  left: 77%;
  top: 72%;
}

.analysis-preview__axis {
  position: absolute;

  z-index: 4;

  color: #6d8189;

  font-family: Georgia, serif;
  font-size: 8px;
}

.analysis-preview__axis--x {
  right: 20px;
  bottom: 10px;
}

.analysis-preview__axis--y {
  left: 14px;
  top: 10px;
}

.analysis-preview__axis--z {
  left: 18px;
  bottom: 13px;
}

.analysis-preview__message {
  position: absolute;

  right: 14px;
  top: 15px;

  display: flex;
  align-items: center;
  gap: 8px;

  color: var(--cyan);
}

.analysis-preview__message strong,
.analysis-preview__message span {
  display: block;
}

.analysis-preview__message strong {
  font-size: 8px;
  font-weight: 600;
}

.analysis-preview__message span {
  margin-top: 3px;

  color: var(--muted);

  font-size: 7px;
}

.analysis-panel__legend {
  min-height: 44px;

  display: flex;
  align-items: center;

  gap: 20px;

  padding: 0 15px;

  color: var(--muted);

  border-top: 1px solid var(--border);

  font-size: 8px;
}

.analysis-panel__legend > div {
  display: flex;
  align-items: center;
  gap: 6px;
}

.analysis-legend {
  width: 14px;
  height: 2px;

  display: inline-block;
}

.analysis-legend--analytical {
  background: var(--cyan);
}

.analysis-legend--numeric {
  background:
    repeating-linear-gradient(
      90deg,
      var(--blue) 0 3px,
      transparent 3px 5px
    );
}

/* RESPONSIVE */

@media (max-width: 1200px) {
  .dashboard__upper {
    grid-template-columns:
      minmax(230px, 0.75fr)
      minmax(460px, 1.6fr);
  }

  .telemetry-panel {
    grid-column: 1 / -1;
    min-height: auto;
  }

  .telemetry-list {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
  }

  .dashboard__lower {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 840px) {
  .topbar {
    height: auto;

    grid-template-columns: 1fr auto;

    padding: 12px 15px;
  }

  .topbar__navigation {
    display: none;
  }

  .brand__subtitle {
    display: none;
  }

  .dashboard {
    padding: 9px;
  }

  .dashboard__upper {
    grid-template-columns: 1fr;
  }

  .control-panel {
    min-height: auto;
  }

  .control-panel__fields {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
  }

  .globe-panel {
    min-height: auto;
  }

  .earth {
    width: min(280px, 66vw);
  }

  .telemetry-list {
    grid-template-columns: repeat(2, 1fr);
  }

  .metrics-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 540px) {
  .system-status__label,
  .system-status__satellite,
  .brand__subtitle {
    display: none;
  }

  .brand__name {
    font-size: 10px;
  }

  .control-panel__fields {
    grid-template-columns: 1fr;
  }

  .globe-stage {
    min-height: 350px;
  }

  .globe-panel__footer {
    gap: 12px;
    flex-wrap: wrap;
  }

  .telemetry-list {
    grid-template-columns: 1fr;
  }

  .metrics-grid {
    grid-template-columns: 1fr;
  }

  .metric-card {
    min-height: 150px;
  }
}

============================================================
ARQUIVO: C:\projetos\satellite-signal-edo-simulator\package.json
============================================================

{
  "name": "satellite-signal-edo-simulator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "^1.46.0",
    "plotly.js": "^4.1.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-globe.gl": "^2.38.0",
    "react-plotly.js": "^4.1.0",
    "satellite.js": "^7.1.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/plotly.js": "^3.0.13",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@types/react-plotly.js": "^2.6.4",
    "@types/three": "^0.186.0",
    "@vitejs/plugin-react": "^6.1.1",
    "oxlint": "^1.81.0",
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  }
}
